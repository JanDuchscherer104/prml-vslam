[meta]
schema_version = 2
repo = "prml-vslam"
updated_on = "2026-04-23"
source = "Resolved or retired issue and todo records moved out of the active backlogs, plus imported review items assessed as already addressed in the current tree or superseded by the current architecture policy."

[[resolved_issues]]
id = "ISSUE-0033"
title = "The internal backlog archive overstates completion for some still-open review themes"
status = "resolved"
priority = "low"
category = "process"
summary = "The backlog re-triage pass is complete: stale imported findings that no longer reproduce on the current tree are now represented only in the resolved archive, and the active backlog no longer carries deleted-supervisor wording."
files = [
    ".agents/issues.toml",
    ".agents/todos.toml",
    ".agents/resolved.toml",
]
notes = [
    "The active Ray smoke issue and todo no longer refer to the removed supervisor actor path.",
    "Previously stale findings such as coordinator restart, missing StageFailed, late RunStopped, latest_backend_event, PlacementPolicy overpromise, Rerun timeline leakage, and keyframe depth logging remain archived as resolved instead of lingering in the active queue.",
    "The active backlog now describes only debt that still reproduces on the current tree.",
]
sources = [
    ".agents/issues.toml",
    ".agents/todos.toml",
    ".agents/resolved.toml",
]
previous_status = "open"
resolved_on = "2026-04-18"
resolution_note = "Closed after backlog re-triage removed stale supervisor-era wording from the active records and aligned the active backlog with the current code."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0049"
title = "Pipeline finalization still carries trivial one-shot projection wrappers"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The single-use summary-projection wrapper is gone; `project_summary()` now constructs stage manifests directly without the extra forwarding hop."
files = [
    "src/prml_vslam/pipeline/finalization.py",
]
notes = [
    "The finalization module now builds `StageManifest` rows inline inside `project_summary()`.",
    "No helper layer remains between `StageOutcome` values and the persisted stage-manifest payload.",
    "The persisted summary and stage-manifest outputs are unchanged by that simplification.",
]
sources = [
    "src/prml_vslam/pipeline/finalization.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "Closed after simplifying summary projection down to one direct manifest-construction path."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0050"
title = "Trajectory evaluation execution currently lives in the pipeline package instead of eval"
status = "resolved"
priority = "medium"
category = "architecture"
summary = "The explicit evo-backed trajectory evaluation implementation now lives under `prml_vslam.eval`, while the coordinator only dispatches the stage and records its typed result."
files = [
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_execution.py",
    "src/prml_vslam/pipeline/finalization.py",
]
notes = [
    "The coordinator now delegates trajectory evaluation through a typed batch-stage executor seam instead of inlining evo-specific work.",
    "The evaluation service owns the method label and persisted metrics artifact construction for pipeline-triggered evaluations.",
    "The finalization module no longer owns a pipeline-local trajectory evaluation helper.",
]
sources = [
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_execution.py",
    "src/prml_vslam/pipeline/finalization.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "Closed after moving trajectory evaluation ownership fully behind the eval package seam and leaving the pipeline with orchestration only."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-PR15-0001"
title = "Packet-session runtime duplication between app and pipeline"
status = "resolved"
priority = "medium"
category = "architecture"
summary = "The older PR15 review flagged duplicated worker-thread and rolling-metrics infrastructure across the app and pipeline, but the current tree already consolidates that logic in `prml_vslam.utils.packet_session`."
files = [
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/pipeline/session.py",
    "src/prml_vslam/utils/packet_session.py",
]
notes = [
    "Both preview controllers and the pipeline session now build on `PacketSessionRuntime`, `PacketSessionMetrics`, and `PacketSessionSnapshot`.",
    "The active debt has moved from duplication to ownership of the app-facing snapshot types layered on top of the shared runtime primitive.",
]
sources = [
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/pipeline/session.py",
    "src/prml_vslam/utils/packet_session.py",
]
previous_status = "imported_review"
resolved_on = "2026-04-05"
resolution_note = "Assessed during PR15 review import; the current tree already shares packet-session lifecycle through `prml_vslam.utils.packet_session`."
source_collection = "review_import"

[[resolved_issues]]
id = "ISSUE-PR15-0002"
title = "Point-cloud PLY writing should delegate to Open3D"
status = "resolved"
priority = "medium"
category = "maintainability"
summary = "The older PR15 review recommended deleting manual ASCII PLY writing. The current tree already routes point-cloud persistence through `write_point_cloud_ply()` in `prml_vslam.utils.geometry`, which uses Open3D."
files = [
    "src/prml_vslam/utils/geometry.py",
    "src/prml_vslam/methods/mock_vslam.py",
]
notes = [
    "The mock SLAM backend already uses the shared geometry helper instead of writing PLY inline.",
    "The helper keeps one explicit empty-cloud fallback for the zero-vertex case, but normal point-cloud export is already library-backed.",
]
sources = [
    "src/prml_vslam/utils/geometry.py",
    "src/prml_vslam/methods/mock_vslam.py",
    "https://www.open3d.org/docs/release/python_api/open3d.io.write_point_cloud.html",
]
previous_status = "imported_review"
resolved_on = "2026-04-05"
resolution_note = "Assessed during PR15 review import; Open3D-backed point-cloud writing is already in place."
source_collection = "review_import"

[[resolved_issues]]
id = "ISSUE-PR15-0003"
title = "Streamlit width-API review threads are stale against the current dependency surface"
status = "resolved"
priority = "low"
category = "review-triage"
summary = "Several unresolved PR15 comments claim that `width=\"stretch\"` is unsupported on Streamlit widgets such as `button`, `plotly_chart`, `link_button`, `dataframe`, `form_submit_button`, `image`, and `segmented_control`, but the current local Streamlit API surface exposes a `width` parameter on those call sites."
files = [
    "pyproject.toml",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/metrics.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/record3d.py",
    "src/prml_vslam/app/live_session.py",
]
notes = [
    "Assessed by inspecting the current local Streamlit function signatures in the working environment.",
    "The comments were not promoted into the active backlog because they do not describe a validated bug in the current tree and environment.",
]
sources = [
    "pyproject.toml",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/metrics.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/record3d.py",
    "src/prml_vslam/app/live_session.py",
]
previous_status = "imported_review"
resolved_on = "2026-04-05"
resolution_note = "Assessed during PR15 comment triage; current local Streamlit signatures accept the flagged `width` parameter, so the review cluster was not promoted."
source_collection = "review_import"

[[resolved_issues]]
id = "ISSUE-0002"
title = "ADVIO calibration parsing depends on undeclared PyYAML"
status = "resolved"
priority = "high"
category = "dependency"
summary = "The ADVIO loader imports yaml directly, but PyYAML is not declared in pyproject.toml core dependencies."
files = [
    "src/prml_vslam/datasets/advio/advio_loading.py",
    "pyproject.toml",
]
notes = [
    "The current environment works only because pyyaml is installed transitively.",
    "Fresh installs should not rely on dependency coincidence for a core dataset path.",
]
previous_status = "open"
resolved_on = "2026-04-05"
resolution_note = "Declared PyYAML as a runtime dependency and added a pyproject regression test."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0001"
title = "BaseConfig-derived workflows are not TOML-first"
status = "resolved"
priority = "high"
category = "configuration"
summary = "User-facing and reproducible BaseConfig graphs should be loaded from TOML, but current durable workflows still rely heavily on direct in-code construction."
files = [
    "src/prml_vslam/utils/base_config.py",
    "src/prml_vslam/utils/path_config.py",
    "src/prml_vslam/main.py",
]
notes = [
    "BaseConfig already exposes from_toml(), to_toml(), and save_toml().",
    "PathConfig already resolves repo-relative .toml paths.",
    "Current CLI entrypoints still build RunRequest graphs directly from flags.",
]
previous_status = "open"
resolved_on = "2026-04-05"
resolution_note = "Added TOML-first CLI run-planning entrypoint plan-run-config with PathConfig + RunRequest.from_toml."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0008"
title = "Metrics evaluation still persists mock results instead of evo outputs"
status = "resolved"
priority = "high"
category = "evaluation"
summary = "The Metrics page and evaluation service still compute a local L2 trajectory-delta mock and persist `mock_metrics.json`, even though the repo README and app requirements describe the surface as explicit `evo` evaluation over persisted results."
files = [
    "README.md",
    "src/prml_vslam/app/REQUIREMENTS.md",
    "src/prml_vslam/eval/README.md",
    "src/prml_vslam/eval/REQUIREMENTS.md",
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/app/pages/metrics.py",
]
notes = [
    "The current implementation computes a direct Euclidean position delta and writes `evaluation/mock_metrics.json`.",
    "The app copy and requirements still describe the action as `Compute evo metrics` and persisted `evo` results.",
    "This leaves the current tree with requirements drift as well as a missing thin adapter to the dependency the repo already ships.",
]
sources = [
    "README.md",
    "src/prml_vslam/app/REQUIREMENTS.md",
    "src/prml_vslam/eval/README.md",
    "src/prml_vslam/eval/REQUIREMENTS.md",
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/app/pages/metrics.py",
    "https://github.com/MichaelGrupp/evo",
]
previous_status = "open"
resolved_on = "2026-04-05"
resolution_note = "Replaced mock metrics with explicit evo APE evaluation and persisted trajectory_metrics.json."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0012"
title = "ADVIO app download flow disagrees with the shared empty-selection contract"
status = "resolved"
priority = "medium"
category = "contracts"
summary = "The shared `AdvioDownloadRequest` contract and CLI already treat `sequence_ids=[]` as 'all scenes', and the app now honors the same contract instead of blocking empty selections."
files = [
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/main.py",
    "tests/test_app.py",
]
notes = [
    "The controller no longer warns on an empty scene selection before calling the dataset service.",
    "The ADVIO download form now explains that leaving the scene selection empty prepares the full catalog.",
    "App tests cover the empty-selection path so the UI stays aligned with the shared request semantics.",
]
sources = [
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/main.py",
    "tests/test_app.py",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15#discussion_r3037245906",
]
previous_status = "open"
resolved_on = "2026-04-05"
resolution_note = "Aligned the app controller and ADVIO page copy with the shared empty-selection-means-all-scenes contract."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-PR15-0004"
title = "Replace repo-owned SE3Pose and TimedPoseTrajectory with evo-native arrays only"
status = "resolved"
priority = "medium"
category = "review-triage"
summary = "The older narrative review recommended deleting the repo-owned SE(3) and trajectory datamodels in favor of plain arrays and `evo`-native objects. That recommendation was not promoted into the active backlog because the current architecture guide explicitly keeps `SE3Pose` and `TimedPoseTrajectory` as the canonical shared datamodels while already delegating TUM I/O to `evo`."
files = [
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/interfaces/camera.py",
    "src/prml_vslam/interfaces/trajectory.py",
    "src/prml_vslam/utils/geometry.py",
]
notes = [
    "The current tree already standardizes TUM trajectory I/O on `evo` while retaining repo-owned typed datamodels for shared boundaries.",
    "This review recommendation conflicts with the current source-of-truth ownership model rather than describing drift inside that model.",
]
sources = [
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/interfaces/camera.py",
    "src/prml_vslam/interfaces/trajectory.py",
    "src/prml_vslam/utils/geometry.py",
    "https://github.com/MichaelGrupp/evo",
]
previous_status = "imported_review"
resolved_on = "2026-04-05"
resolution_note = "Retired during the narrative PR15 review triage because the current repo policy intentionally preserves these shared datamodels."
source_collection = "review_import"

[[resolved_issues]]
id = "ISSUE-PR15-0005"
title = "Turn methods and eval into the repo-owned home for full upstream orchestration"
status = "resolved"
priority = "medium"
category = "review-triage"
summary = "The older narrative review argued for replacing the local mock `methods` and `eval` surfaces with the repository-owned home for direct upstream method orchestration and a larger evaluation layer. That broad restructuring was not promoted as an active issue because the current package rules intentionally scope `methods` and `eval` as thin local interface/mock surfaces; only the narrower overlap and placeholder debts remain active."
files = [
    "README.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/methods/README.md",
    "src/prml_vslam/eval/README.md",
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/eval/contracts.py",
]
notes = [
    "The still-relevant parts of that review are already represented in active backlog items such as duplicate normalized-output ownership, placeholder controls, bespoke ADVIO download glue, and the custom Record3D Wi-Fi boundary.",
    "If repo scope later expands to direct upstream orchestration, that should be recorded as a fresh scope change instead of implicitly reopening this older recommendation.",
]
sources = [
    "README.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/methods/README.md",
    "src/prml_vslam/eval/README.md",
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/eval/contracts.py",
    "https://github.com/zhangganlin/vista-slam",
    "https://github.com/rmurai0610/MASt3R-SLAM",
    "https://github.com/MichaelGrupp/evo",
]
previous_status = "imported_review"
resolved_on = "2026-04-05"
resolution_note = "Retired during the narrative PR15 review triage because the current repo policy keeps `methods` and `eval` intentionally thin unless project scope changes."
source_collection = "review_import"

[[resolved_issues]]
id = "ISSUE-0006"
title = "Placeholder config and state fields remain in active models"
status = "resolved"
priority = "medium"
category = "cleanup"
summary = "Several active config models still carry shape-only fields and wrappers that add maintenance cost without current value."
files = [
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/methods/mock_vslam.py",
    "README.md",
    "tests/test_main.py",
    "tests/test_methods.py",
]
notes = [
    "The `kind` discriminator on source-spec configs is shape-only boilerplate in the current tree because the three source models are already structurally distinct.",
    "Record3DUSBPacketStreamConfig still wraps Record3DStreamConfig through a one-field `stream` indirection.",
    "MockSlamBackendConfig.method_id is currently validated and forwarded even though the mock backend does not use it.",
    "Tests and TOML examples still encode some of the older boilerplate surface and should converge with the simplified contracts.",
]
sources = [
    "README.md",
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/methods/mock_vslam.py",
    "tests/test_main.py",
    "tests/test_methods.py",
]
previous_status = "open"
resolved_on = "2026-04-05"
resolution_note = "Removed the source discriminator fields, flattened the USB packet-stream config, and dropped the unused mock backend method_id config field."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0015"
title = "SessionStateStore repeats per-runtime load, compatibility, and replacement flows"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The Streamlit session-state adapter still reimplements the same get-or-create, compatibility-check, stale-runtime shutdown, and replacement path for Record3D, ADVIO, and Pipeline runtimes. This matches the older review's app-heaviness concern and leaves a concrete duplication hotspot even after packet-session lifecycle was consolidated."
files = [
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
notes = [
    "The three runtime loaders differ mostly in the constructor and required-method predicate.",
    "A generic helper would reduce app boilerplate while keeping the dedicated typed controllers and pipeline service intact.",
]
sources = [
    "src/prml_vslam/app/REQUIREMENTS.md",
    "src/prml_vslam/app/AGENTS.md",
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
previous_status = "open"
resolved_on = "2026-04-05"
resolution_note = "SessionStateStore now uses one generic loader for session-state runtime lookup, compatibility checks, stale-runtime shutdown, and replacement."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0007"
title = "SessionStateStore stale-runtime replacement paths lack focused tests"
status = "resolved"
priority = "low"
category = "testing"
summary = "The session-state adapter can replace incompatible runtimes, but the current test coverage only verifies a basic pipeline-runtime round trip."
files = [
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
notes = [
    "Compatibility fallback and stale-runtime shutdown behavior are implemented but not directly covered.",
]
previous_status = "open"
resolved_on = "2026-04-05"
resolution_note = "Focused app tests now cover stale SessionStateStore runtime replacement for record3d, ADVIO, and pipeline adapters."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0003"
title = "Live preview snapshot contracts are not owned consistently by app models"
status = "resolved"
priority = "medium"
category = "architecture"
summary = "App-facing preview snapshots still live outside `prml_vslam.app.models`: `Record3DStreamSnapshot` is owned by `prml_vslam.io.record3d`, while `AdvioPreviewSnapshot` and `AdvioPreviewStreamState` are still defined in `prml_vslam.app.services`."
files = [
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pages/record3d.py",
    "tests/test_app.py",
]
notes = [
    "The Streamlit app imports these runtime snapshots directly for rendering and test doubles.",
    "This still blurs two repo rules: app-facing state belongs under `prml_vslam.app.models`, and `services.py` should not own public contract types.",
]
sources = [
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/app/AGENTS.md",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/models.py",
    "tests/test_app.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Moved Record3D and ADVIO preview snapshot contracts into prml_vslam.app.models and stopped exporting them from transport/service modules."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0004"
title = "Page-state persistence helpers are duplicated across app surfaces"
status = "resolved"
priority = "medium"
category = "maintainability"
summary = "Multiple app modules repeat near-identical save-if-changed logic for typed page state."
files = [
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/record3d_controller.py",
]
notes = [
    "The duplication is small but repeated enough to encourage drift in state handling.",
    "A generic helper should preserve the typed models while removing controller boilerplate.",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Removed the remaining one-off page-state save wrappers and switched Record3D action handling to the shared save_model_updates path."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0005"
title = "FramePacket and SlamUpdate still overlap ingress and preview semantics"
status = "resolved"
priority = "medium"
category = "contracts"
summary = "The canonical ingress packet still carries preview- and method-oriented fields (`pointmap`, `uncertainty`, `image_path`, `jpeg_bytes`) while `SlamUpdate` also owns `pointmap` and `uncertainty`, leaving the current tree with overlapping owners and a live sensor-confidence naming collision."
files = [
    "src/prml_vslam/interfaces/runtime.py",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/methods/mock_vslam.py",
    "src/prml_vslam/app/pages/record3d.py",
    "src/prml_vslam/app/pages/pipeline.py",
]
notes = [
    "Record3D currently maps USB depth confidence into `FramePacket.uncertainty`, even though `SlamUpdate.uncertainty` reads like method output.",
    "The mock SLAM session also consumes `FramePacket.pointmap` while emitting `SlamUpdate.pointmap`, so the same semantic concept still has two owners.",
    "FramePacket.image_path and FramePacket.jpeg_bytes remain unused in the current repository tree.",
]
sources = [
    "README.md",
    "docs/Questions.md",
    "src/prml_vslam/app/REQUIREMENTS.md",
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/interfaces/runtime.py",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/methods/mock_vslam.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "FramePacket now carries sensor confidence instead of uncertainty, ingress no longer owns pointmaps, and SlamUpdate is the sole pointmap owner."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0009"
title = "Method wrapper contracts still duplicate pipeline artifact ownership"
status = "resolved"
priority = "medium"
category = "architecture"
summary = "The methods layer still exposes `MethodRunRequest` and `MethodRunResult` as public normalized-output contracts even though the architecture guide already treats `SequenceManifest`, `SlamArtifacts`, and `RunSummary` as the repository-owned artifact boundary."
files = [
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/methods/base.py",
    "tests/test_methods.py",
]
notes = [
    "The mock method runtime still plans and materializes normalized trajectory and point-cloud paths directly through `MethodRunResult`.",
    "The architecture guide explicitly calls this overlap transitional and says wrapper outputs should converge on pipeline-owned artifacts.",
    "The broader older review recommendation to keep upstream wrappers thin is still relevant here even though the current repository scope intentionally keeps `methods` mock-only.",
]
sources = [
    "README.md",
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/methods/README.md",
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/methods/base.py",
    "tests/test_methods.py",
    "https://github.com/zhangganlin/vista-slam",
    "https://github.com/rmurai0610/MASt3R-SLAM",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Mock method runtimes now emit pipeline-owned SlamArtifacts directly instead of a parallel MethodRunResult contract."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0010"
title = "Package re-export surfaces remain broader than the minimal public API"
status = "resolved"
priority = "medium"
category = "api"
summary = "The `io` and `pipeline` package roots still re-export transport internals, snapshot types, signaling helpers, and session classes beyond the minimal public surface documented in the architecture rules."
files = [
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/io/__init__.py",
    "src/prml_vslam/pipeline/__init__.py",
]
notes = [
    "`prml_vslam.io.__init__` still exports transport sessions, Wi-Fi signaling helpers, and the Record3D snapshot type.",
    "`prml_vslam.pipeline.__init__` still exports `PipelineSessionService`, `PipelineSessionSnapshot`, and `PipelineSessionState` via a dynamic import bridge even though the documented minimal public surface is narrower.",
]
sources = [
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/io/__init__.py",
    "src/prml_vslam/pipeline/__init__.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "The io and pipeline package roots already match the documented minimal API, and package-export tests now guard against broader re-exports."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0011"
title = "ADVIO download flow still owns bespoke cache and fetch mechanics"
status = "resolved"
priority = "medium"
category = "maintainability"
summary = "The ADVIO download manager still owns low-level HTTP download, temp-file replacement, checksum validation, and ZIP extraction logic directly instead of delegating archive retrieval and cache handling to a dedicated dataset helper."
files = [
    "src/prml_vslam/datasets/README.md",
    "src/prml_vslam/datasets/advio/advio_download.py",
    "pyproject.toml",
]
notes = [
    "The current implementation manually combines `urllib.request`, `tempfile`, `hashlib`, and `zipfile` for archive retrieval and caching.",
    "The archive-cache correctness bug has been fixed locally, but the surrounding retrieval and cache-management path is still bespoke.",
    "ADVIO upstream already publishes per-sequence ZIP files on Zenodo, which fits a registry-based data fetcher well.",
    "A library such as Pooch would let the repo keep its typed catalog and local-readiness logic while shrinking the bespoke download path.",
]
sources = [
    "src/prml_vslam/datasets/README.md",
    "src/prml_vslam/datasets/advio/advio_download.py",
    "pyproject.toml",
    "https://github.com/AaltoVision/ADVIO",
    "https://zenodo.org/record/1320824",
    "https://www.fatiando.org/pooch/latest/",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15#discussion_r3037245887",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "ADVIO archive and calibration retrieval now flow through a shared DatasetFetchHelper backed by pooch instead of bespoke HTTP and checksum glue in the download manager."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0013"
title = "Source-of-truth docs still contain stale packet names, broken links, and placeholder TODO text"
status = "resolved"
priority = "low"
category = "documentation"
summary = "Several repo docs still drift from the current code surface: `Record3DFramePacket` remains in Record3D docs, `app/AGENTS.md` still has a quoted markdown target, and `docs/Questions.md` still contains an unresolved ARCore placeholder TODO."
files = [
    "src/prml_vslam/io/README.md",
    "src/prml_vslam/app/README.qmd",
    "src/prml_vslam/app/AGENTS.md",
    "docs/Questions.md",
]
notes = [
    "The shared runtime packet contract is `FramePacket`, not `Record3DFramePacket`.",
    "The quoted markdown target in `app/AGENTS.md` renders as a broken link on GitHub.",
    "The Q&A document is one of the repo sources of truth, so leaving placeholder TODO text there increases requirements drift.",
]
sources = [
    "src/prml_vslam/io/README.md",
    "src/prml_vslam/app/README.qmd",
    "src/prml_vslam/app/AGENTS.md",
    "docs/Questions.md",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15#discussion_r3036947625",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15#discussion_r3037245891",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Source-of-truth docs now use FramePacket, the app plotting link resolves correctly, and the placeholder ARCore/Nerfstudio text has been replaced with concrete wording."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0014"
title = "Record3D Wi-Fi remains a repo-owned first-class transport beside the official USB bindings"
status = "resolved"
priority = "medium"
category = "architecture"
summary = "The repository already uses the official `record3d` Python bindings for USB capture, but the app and IO layers still treat a custom HTTP/WebRTC Wi-Fi receiver as a peer transport. That leaves signaling, peer lifecycle, packet decoding, and transport-specific UI state as permanent-looking repo code even though Record3D documents USB streaming as the Python/C++ integration surface and presents Wi-Fi separately as a browser/local-network feature."
files = [
    "README.md",
    "docs/Questions.md",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/io/wifi_session.py",
    "src/prml_vslam/io/wifi_signaling.py",
    "src/prml_vslam/io/wifi_receiver.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/pages/record3d.py",
]
notes = [
    "USB capture already flows through the upstream `record3d` bindings, so the custom Wi-Fi stack is a qualitatively different maintenance obligation than the official adapter.",
    "The older review recommendation is still relevant in the current tree: either keep Wi-Fi clearly optional/private, or justify it as an explicit separate boundary for the real-time streaming requirement recorded in `docs/Questions.md`.",
]
sources = [
    "README.md",
    "docs/Questions.md",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/io/wifi_session.py",
    "src/prml_vslam/io/wifi_signaling.py",
    "src/prml_vslam/io/wifi_receiver.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/pages/record3d.py",
    "https://record3d.app/",
    "https://github.com/marek-simonik/record3d",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Record3D Wi-Fi now uses preview-scoped entry points and app wording, while README and requirements make USB the canonical programmatic integration boundary."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0015"
title = "Trajectory plotting remains fragmented across multiple near-overlapping builders"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The plotting layer currently spreads trajectory rendering across `metrics.py`, `record3d.py`, `trajectories.py`, and `pipeline.py`, with repeated trace assembly, marker, and layout logic instead of one shared internal builder."
files = [
    "src/prml_vslam/plotting/metrics.py",
    "src/prml_vslam/plotting/record3d.py",
    "src/prml_vslam/plotting/trajectories.py",
    "src/prml_vslam/plotting/pipeline.py",
    "src/prml_vslam/app/live_session.py",
]
notes = [
    "The figure types are not identical, but XY overlays, 3D trajectory traces, start/end markers, and standard axis/layout setup still repeat enough to drift.",
    "The app already centralizes live-session rendering in `app/live_session.py`, so plotting internals are the remaining main place where trajectory presentation is split by page.",
]
sources = [
    "src/prml_vslam/plotting/metrics.py",
    "src/prml_vslam/plotting/record3d.py",
    "src/prml_vslam/plotting/trajectories.py",
    "src/prml_vslam/plotting/pipeline.py",
    "src/prml_vslam/app/live_session.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Trajectory plotting now shares internal XY/3D trace and layout helpers across the metrics, live Record3D, and pipeline figures instead of repeating the scaffolding per module."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0016"
title = "Preview session utilities still carry removable boilerplate"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The shared packet-session snapshot defaults still rely on dedicated empty-array wrapper functions, and the Record3D runtime still threads its connected source label through a mutable holder plus helper even though local worker state is sufficient."
files = [
    "src/prml_vslam/utils/packet_session.py",
    "src/prml_vslam/app/services.py",
]
notes = [
    "The empty-array factories in `packet_session.py` are only there to satisfy `Field(default_factory=...)` and can be replaced with copy-based defaults.",
    "The Record3D worker currently mutates a `source_label_holder` dict and calls `_set_record3d_connected_state()` even though `_run_stream_worker()` already owns the connected-target lifetime.",
]
sources = [
    "src/prml_vslam/utils/packet_session.py",
    "src/prml_vslam/app/services.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Removed empty-array wrapper helpers from packet_session and inlined Record3D source-label updates into the worker."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0017"
title = "App preview state ownership is split across duplicate enums"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The app models still define separate `Record3DStreamState` and `AdvioPreviewStreamState` enums even though both surfaces expose the same lifecycle values and transitions."
files = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/record3d_controller.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/record3d.py",
]
notes = [
    "The duplicated enums force page, controller, and test code to carry two names for one lifecycle contract.",
    "The current app requirements describe both surfaces as preview-oriented monitoring flows, so one shared app-owned preview state is sufficient.",
]
sources = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/record3d_controller.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/record3d.py",
    "src/prml_vslam/app/REQUIREMENTS.md",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Replaced the duplicate Record3D and ADVIO preview-state enums with one shared PreviewStreamState across the app layer."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0018"
title = "Geometry TUM helpers still pretend evo is optional"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The shared geometry module still routes TUM trajectory I/O through a local lazy-import helper even though `evo` is a declared runtime dependency for the repository."
files = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
notes = [
    "The helper adds extra indirection and error wrapping without protecting a genuinely optional dependency.",
    "Repo policy prefers thin, explicit ownership boundaries and early failure when a required dependency is missing.",
]
sources = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Removed the redundant lazy evo import helper and imported the TUM backend directly in geometry.py."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0019"
title = "World-frame point transforms still reimplement pytransform3d primitives"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The shared geometry helper still hand-builds homogeneous points for `transform_points_world_camera()` even though `pytransform3d` is already a declared dependency and exposes the same transform primitive."
files = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
notes = [
    "The current implementation duplicates library behavior with local homogeneous-coordinate scaffolding.",
    "Using the repo's existing transform helper should narrow local ownership to validation plus repo-specific typing.",
]
sources = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Delegated world-frame point transforms to pytransform3d instead of rebuilding homogeneous coordinates locally."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0020"
title = "App preview snapshot ownership still repeats shared state fields"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The app models still repeat the same `state` field across both preview snapshot types, and both controllers still repeat the same active-state set."
files = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/record3d_controller.py",
]
notes = [
    "The shared preview lifecycle already has one enum; the snapshot base and active-state constant can follow the same ownership model.",
    "This is local app boilerplate rather than user-facing behavior, so it is a good simplification target for the bounded app test harness.",
]
sources = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/record3d_controller.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Centralized the shared preview state field and active-state set under the app models module."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0021"
title = "Remaining geometry TODO markers are stale against current dependency usage"
status = "resolved"
priority = "low"
category = "repo-hygiene"
summary = "The last two TODO markers in `geometry.py` no longer describe live debt: point-cloud writing already delegates to Open3D except for the required empty-cloud fallback, and dense depth unprojection has no matching helper in the current pytransform3d camera API."
files = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
notes = [
    "Open3D fails to materialize empty point clouds, so the repo-owned zero-vertex fallback is intentional rather than a missed delegation.",
    "The current pytransform3d camera module exposes projection helpers such as `cam2sensor` and `sensor2img`, but not a dense inverse-unprojection helper that would replace `pointmap_from_depth()`.",
]
sources = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Retired the remaining stale geometry TODO markers after validating the current Open3D and pytransform3d surfaces."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0022"
title = "Methods package still splits mock configs across per-backend wrapper modules"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The methods package still exports `MSTRMethodConfig` and `VISTAMethodConfig` as separate one-line subclasses in dedicated modules even though the repo only needs one typed mock config plus `MethodId` to build the deterministic placeholder runtime."
files = [
    "src/prml_vslam/methods/__init__.py",
    "src/prml_vslam/methods/base.py",
    "src/prml_vslam/methods/mstr.py",
    "src/prml_vslam/methods/vista.py",
    "tests/test_methods.py",
]
notes = [
    "The wrapper modules add public surface and file count without adding behavior beyond binding one enum value.",
    "The only in-repo consumer is the mock-method test, so the extra subclasses are local boilerplate rather than a required compatibility seam.",
]
sources = [
    "src/prml_vslam/methods/__init__.py",
    "src/prml_vslam/methods/base.py",
    "src/prml_vslam/methods/mstr.py",
    "src/prml_vslam/methods/vista.py",
    "tests/test_methods.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Collapsed the methods mock config surface into one typed MockMethodConfig and removed the one-line per-backend wrapper modules."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0023"
title = "App bootstrap still hard-codes page entry wrappers and a dead style hook"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The app bootstrap still carries four page-specific entry wrappers plus a no-op `inject_styles()` hook even though page registration can be expressed as one small registry and one generic entry helper."
files = [
    "src/prml_vslam/app/bootstrap.py",
    "src/prml_vslam/app/ui.py",
    "tests/test_app.py",
]
notes = [
    "The page-entry wrappers differ only by page id and renderer function.",
    "The style injection hook does nothing and only adds an import and call path to the bootstrap flow.",
]
sources = [
    "src/prml_vslam/app/bootstrap.py",
    "src/prml_vslam/app/ui.py",
    "tests/test_app.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Replaced the page-specific bootstrap entry wrappers with one page registry and generic entry helper, and removed the dead inject_styles hook."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0024"
title = "ADVIO pose-source display labels are duplicated across app pages"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The ADVIO app pages and metrics launcher still each own their own pose-source display mapping even though `AdvioPoseSource` is the canonical contract and can own that label directly."
files = [
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/metrics.py",
]
notes = [
    "The duplicated label tables create avoidable app-side drift for one repo-owned enum.",
    "The metrics launcher was already formatting the same enum by munging the raw value string instead of using a contract-owned label.",
]
sources = [
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/metrics.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Moved the display label onto AdvioPoseSource and deleted the duplicated page-local mappings."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0025"
title = "Pipeline mode display text still lives in a page-local helper"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The pipeline page still owned the user-facing display text for `PipelineMode` even though `PipelineMode` is the canonical contract and other app-facing enums already own their labels directly."
files = [
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/app/pages/pipeline.py",
]
notes = [
    "The local `_pipeline_mode_label()` helper was only restating enum-owned semantics in page code.",
    "The page also carried a one-line `_is_pipeline_active()` wrapper that did not add ownership value beyond the active-state constant.",
]
sources = [
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/app/pages/pipeline.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Moved the display label onto PipelineMode and inlined the one-line activity helper in the pipeline page."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0026"
title = "DatasetId still carries single-case ADVIO dispatch boilerplate"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The repo is now ADVIO-only on the dataset surface, but `DatasetId` still kept one-entry dispatch helpers for labels, local sequence listing, and reference path resolution."
files = [
    "src/prml_vslam/datasets/contracts.py",
]
notes = [
    "The enum-backed label helper duplicated the raw enum value for the only supported dataset.",
    "Single-case dispatch in DatasetId helpers adds structure without buying extensibility on the current evaluation surface.",
]
sources = [
    "src/prml_vslam/datasets/contracts.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Collapsed the one-case label and ADVIO dispatch helpers into direct enum/value-backed behavior."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0027"
title = "Several enum label helpers still restate trivially derivable values"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "A few app and dataset enums still own full mapping tables even though most labels are already a direct capitalization or uppercasing of the enum value."
files = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/io/record3d.py",
]
notes = [
    "Full enum-to-label tables add boilerplate when only one or two entries are true special cases.",
    "Keeping the special cases explicit while deriving the default shape from the enum value preserves the contract with less code.",
]
sources = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/io/record3d.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Collapsed mostly-derived enum label mappings into value-based formatting while preserving the explicit special cases."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0028"
title = "MethodId still exposes an identity artifact_slug wrapper"
status = "resolved"
priority = "low"
category = "maintainability"
summary = "The planner and run-discovery code still went through `MethodId.artifact_slug` even though the method enum value already is the artifact slug used in artifact paths."
files = [
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/pipeline/services.py",
    "src/prml_vslam/eval/services.py",
    "tests/test_pipeline.py",
]
notes = [
    "The wrapper added an extra contract surface without any transformation logic.",
    "Switching call sites to `MethodId.value` keeps artifact layout behavior unchanged while shrinking the enum API.",
]
sources = [
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/pipeline/services.py",
    "src/prml_vslam/eval/services.py",
    "tests/test_pipeline.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Removed the identity wrapper and updated planning and run-discovery code to use the enum value directly."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0029"
title = "Pipeline demo session still acts as the de facto runner API"
status = "resolved"
priority = "high"
category = "architecture"
summary = "The current runtime remains centered on `PipelineSessionService` plus app-owned demo launch helpers, even though the pipeline requirements call for a `RunService` facade over explicit offline and streaming runners."
files = [
    "src/prml_vslam/pipeline/session.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/metrics.py",
    "src/prml_vslam/app/pipeline_demo.py",
    "src/prml_vslam/pipeline/REQUIREMENTS.md",
]
notes = [
    "`PipelineSessionService` still hard-rejects planner stages outside `INGEST`/`SLAM`/`SUMMARY` and constructs the mock backend through its default factory.",
    "Both the `Pipeline` and `Metrics` pages can launch runs directly, so orchestration semantics are still split across app pages instead of sitting behind one pipeline-facing facade.",
    "This is now the main surviving architectural finding from the older PR15 reviews.",
]
sources = [
    "src/prml_vslam/pipeline/session.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/metrics.py",
    "src/prml_vslam/app/pipeline_demo.py",
    "src/prml_vslam/pipeline/REQUIREMENTS.md",
    "src/prml_vslam/app/REQUIREMENTS.md",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Introduced RunService as the app-facing facade, removed Metrics-page run launching, and narrowed PipelineSessionService to bounded session execution over pre-resolved plans and backends."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0034"
title = "PathConfig hidden-root policy was only partially propagated through the repo"
status = "resolved"
priority = "high"
category = "regression"
summary = "The intentional move to hidden `.artifacts` and `.data` roots landed in `PathConfig`, but the repo still had stale tests, CLI defaults, and examples that assumed `artifacts` and `data`, which broke dataset discovery, persisted-run lookup, and path-config regression coverage."
files = [
    "src/prml_vslam/utils/path_config.py",
    "README.md",
    "src/prml_vslam/datasets/README.md",
    "tests/test_path_config.py",
    "tests/test_advio.py",
    "tests/test_app.py",
]
notes = [
    "The hidden-root change was intentional, but the CLI, README examples, dataset README examples, and tests still consistently assumed `artifacts/` and `data/` as the repo-owned default roots.",
    "Because ADVIO services and metrics discovery consume `PathConfig` transitively, the partial migration surfaced as missing local sequences, missing cached archives, and missing reference trajectories rather than as a direct validator error.",
    "The correct fix was to propagate the hidden-root policy through tests and user-facing defaults instead of reverting `PathConfig`.",
]
sources = [
    "src/prml_vslam/utils/path_config.py",
    "README.md",
    "src/prml_vslam/datasets/README.md",
    "tests/test_path_config.py",
    "tests/test_advio.py",
    "tests/test_app.py",
]
previous_status = "open"
resolved_on = "2026-04-06"
resolution_note = "Propagated the intentional `.artifacts`/`.data` policy through tests, CLI defaults, and examples so hidden roots are the consistent repo-owned layout again."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0034"
title = "Offline and unsupported planned stages still need the process-aware stage-result executor"
status = "resolved"
priority = "high"
category = "orchestration"
summary = "Streaming execution now has a pipeline-owned coordinator and typed stage results, but offline execution and the still-unsupported planned stages continue to rely on the older boolean/finalization model."
files = [
    "src/prml_vslam/pipeline/contracts/execution.py",
    "src/prml_vslam/pipeline/offline.py",
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/pipeline/run_service.py",
    "tests/test_pipeline.py",
]
notes = [
    "Streaming mode now uses `StageExecutionMode`, `StageResult`, and `StreamingCoordinator` to run ingest, packet-source, SLAM, evaluation, and summary through explicit execution records.",
    "`OfflineRunner._run_worker` still tracks progress through booleans such as `ingest_started` and `slam_started` rather than consuming stage results.",
    "The legacy finalization path still exists for offline execution, so the older boolean/finalization model is not fully removed from the pipeline package.",
    "Reference reconstruction, cloud evaluation, and efficiency stages remain plannable but unsupported by the executable slice.",
]
sources = [
    "README.md",
    "docs/Questions.md",
    "src/prml_vslam/pipeline/REQUIREMENTS.md",
    "src/prml_vslam/pipeline/contracts/execution.py",
    "src/prml_vslam/pipeline/offline.py",
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/pipeline/run_service.py",
]
previous_status = "open"
resolved_on = "2026-04-16"
resolution_note = "Superseded by the Ray-backed pipeline refactor; the old offline/process-stage executor path and its boolean finalization model were removed from the active architecture."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0036"
title = "Repo-owned Rerun transform relation contradicts the canonical camera pose direction"
status = "resolved"
priority = "high"
category = "visualization"
summary = "The repo stores camera poses as `T_world_camera`, but `visualization.rerun.log_transform()` logs them as `rr.TransformRelation.ChildFromParent`, which reverses the declared pose direction and is the strongest current explanation for misplaced camera-local pointmaps and incorrect world unprojection in the repo-owned viewer path."
files = [
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/interfaces/transforms.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/streaming.py",
    "src/prml_vslam/pipeline/streaming_coordinator.py",
    "tests/test_streaming_visualization.py",
    "src/prml_vslam/visualization/README.md",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
]
notes = [
    "`FrameTransform` and the package guidance define the canonical camera pose convention as world <- camera (`T_world_camera`).",
    "`log_transform()` emits that pose with `rr.TransformRelation.ChildFromParent`, while `streaming_coordinator` logs camera-local pointmaps under posed keyframe entities that inherit the transform.",
    "`streaming._viewer_pose_from_update()` also applies a ViSTA-specific basis flip before logging, so transform-direction mistakes and viewer-basis conversion are currently coupled in one path.",
    "Current streaming visualization tests assert entity paths, timelines, and basis values, but they do not round-trip a known camera-local point into an expected world-space location.",
]
sources = [
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/interfaces/transforms.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/streaming.py",
    "src/prml_vslam/pipeline/streaming_coordinator.py",
    "tests/test_streaming_visualization.py",
    "src/prml_vslam/visualization/README.md",
]
previous_status = "open"
resolved_on = "2026-04-16"
resolution_note = "Resolved by the visualization refactor: repo transforms now log with parent-from-child semantics, so the original pose-direction contradiction is no longer present."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0038"
title = "Residual compatibility shims and stale cutover docs remain after the Ray migration"
status = "resolved"
priority = "medium"
category = "simplification"
summary = "Most of the Ray/event-first cutover is in place, but a few transition surfaces still preserve redundant or stale architecture state: a compatibility re-export in `pipeline.state`, duplicate backend identity in `RunPlan`, a dead process-era ViSTA session factory, and docs that still mention removed runner files."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/pipeline/state.py",
    "src/prml_vslam/pipeline/contracts/plan.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/visualization/README.md",
    ".agents/AGENTS_INTERNAL_DB.md",
]
notes = [
    "`pipeline.state` is now only a compatibility re-export over `contracts.runtime`.",
    "`RunPlan` still carries both `method` and `backend_kind`, even though `backend.kind` is now the actual source of truth for backend selection.",
    "`VistaSlamBackend.start_session()` still goes through `_VistaSessionFactory`, and the same file still carries a TODO noting that one pure rotation helper should live in a generic owner instead.",
    "Some internal or package docs still describe removed pre-Ray surfaces such as the old session/runner split, which increases confusion for later agents and reviewers.",
]
sources = [
    "src/prml_vslam/pipeline/state.py",
    "src/prml_vslam/pipeline/contracts/plan.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/visualization/README.md",
    ".agents/AGENTS_INTERNAL_DB.md",
]
previous_status = "in_progress"
resolved_on = "2026-04-16"
resolution_note = "Removed the active post-cutover compatibility shims called out by the issue: pipeline.state is gone, RunPlan no longer duplicates backend identity, the ViSTA process-era session-factory path was deleted, and the maintained pipeline/visualization docs plus AGENTS_INTERNAL_DB now describe the current Ray/event-first architecture."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0031"
title = "Ingress and live-source contracts still rely on untyped packet provenance and duplicate transport enums"
status = "resolved"
priority = "medium"
category = "contracts"
summary = "The repo removed the earlier `FramePacket` overlap, but the hot-path contracts still push replay and transport provenance through generic metadata and duplicate Record3D transport identity across pipeline and app/io enums."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/interfaces/runtime.py",
    "src/prml_vslam/io/cv2_producer.py",
    "src/prml_vslam/datasets/advio/advio_replay_adapter.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/app/pipeline_controller.py",
]
notes = [
    "`FramePacket.metadata` still carries replay, dataset, transport, device-address, and rotation provenance in a generic `dict[str, Any]` blob.",
    "Replay and dataset adapters currently depend on `static_metadata` plumbing instead of a typed source/provenance contract.",
    "The live-source boundary is no longer a free-form source string, but transport identity is still split between `LiveTransportId` in pipeline contracts and `Record3DTransportId` in app/io surfaces.",
]
sources = [
    "src/prml_vslam/interfaces/runtime.py",
    "src/prml_vslam/io/cv2_producer.py",
    "src/prml_vslam/datasets/advio/advio_replay_adapter.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/REQUIREMENTS.md",
]
previous_status = "in_progress"
resolved_on = "2026-04-16"
resolution_note = "Closed after replacing FramePacket metadata with typed provenance and anchoring Record3D transport identity in the shared runtime contract."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0035"
title = "Reusable pure helpers remain trapped in leaf UI and method-wrapper modules"
status = "resolved"
priority = "medium"
category = "simplification"
summary = "Several generally useful artifact, image, and frame-materialization helpers live in Streamlit page modules, pipeline leaf modules, or the ViSTA adapter instead of reusable package-owned modules."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/live_session.py",
    "src/prml_vslam/pipeline/ingest.py",
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/utils/REQUIREMENTS.md",
]
notes = [
    "`app/pages/pipeline.py` previously owned request-building, source adaptation, support checks, evo preview, artifact display, and pointmap-preview image conversion in one large page module; request/source logic now lives in an app controller and evo/preview conversion moved to eval/plotting.",
    "Frame extraction exists in both `pipeline.ingest` and `methods.vista.adapter`, even though generic video-to-frame materialization should be shared or performed once before method adapters run.",
    "JSON writing, stable hashing, stage-manifest output-path assembly, and live image normalization are reusable mechanics but currently live near specific workflow leaves.",
    "The cleanup should move pure helpers to existing owners such as `pipeline`, `visualization`, `plotting`, or `utils` only when the helper is genuinely generic and reduces the leaf surface.",
]
sources = [
    "src/prml_vslam/utils/REQUIREMENTS.md",
    "src/prml_vslam/app/AGENTS.md",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/live_session.py",
    "src/prml_vslam/pipeline/ingest.py",
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/methods/vista/adapter.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-16"
resolution_note = "Closed after extracting shared video-frame materialization and moving generic SO(3) projection out of the ViSTA adapter."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0036"
title = "Rerun pose-direction semantics need regression coverage after the ParentFromChild cutover"
status = "resolved"
priority = "high"
category = "visualization"
summary = "The current code now logs repo `T_world_camera` poses with `ParentFromChild`, but the repo still lacks a focused world-space round-trip test to lock in that transform direction and prevent regressions."
files = [
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/interfaces/transforms.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "tests/test_streaming_visualization.py",
    "src/prml_vslam/visualization/README.md",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
]
notes = [
    "`FrameTransform` and the package guidance define the canonical camera pose convention as world <- camera (`T_world_camera`).",
    "`visualization.rerun.log_transform()` now uses `rr.TransformRelation.ParentFromChild`, so the original direction bug appears fixed in the current worktree.",
    "What remains missing is a focused regression test that round-trips a known camera-local point through a known pose and fails if world placement becomes mirrored, inverted, or displaced again.",
    "The active backlog had drifted toward removed pre-Ray streaming files, so the remaining visualization debt should now be tracked against the sink/helper path that actually logs poses.",
]
sources = [
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/interfaces/transforms.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "tests/test_streaming_visualization.py",
    "src/prml_vslam/visualization/README.md",
]
previous_status = "open"
resolved_on = "2026-04-16"
resolution_note = "Closed after adding focused Rerun transform-direction regression coverage and keeping ParentFromChild as the single repo-owned pose relation path."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0037"
title = "Camera-complete live payloads exist in contracts, but the Rerun sink still ignores them"
status = "resolved"
priority = "medium"
category = "visualization"
summary = "The method/runtime layer now carries camera intrinsics, RGB, and metric depth for live keyframes, but the repo-owned Rerun sink still only logs pose and preview imagery, so the camera-complete payload never reaches the viewer path end-to-end."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/methods/updates.py",
    "src/prml_vslam/methods/events.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/pipeline/ray_actors.py",
    "tests/test_methods.py",
    "tests/test_streaming_visualization.py",
    "src/prml_vslam/visualization/README.md",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
]
notes = [
    "`SlamUpdate` now carries `camera_intrinsics`, `image_rgb`, and `depth_map`, and `translate_slam_update()` already threads image/depth handles through the explicit backend-event surface.",
    "`RerunEventSink.observe()` still ignores the available `PreviewReady.image` and `PreviewReady.depth` handles, so the viewer path does not yet log RGB plus `DepthImage` plus `Pinhole` together.",
    "The current visualization test surface only checks a no-op path when Rerun is unavailable, not the camera-complete payload wiring through the sink and handle resolver.",
    "The remaining gap is now mostly in the sink plus end-to-end coverage rather than in `SlamUpdate` itself.",
]
sources = [
    "src/prml_vslam/methods/updates.py",
    "src/prml_vslam/methods/events.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/pipeline/ray_actors.py",
    "tests/test_methods.py",
    "tests/test_streaming_visualization.py",
    "src/prml_vslam/visualization/README.md",
]
previous_status = "in_progress"
resolved_on = "2026-04-16"
resolution_note = "Closed after wiring intrinsics, RGB, and metric depth through explicit backend events into the repo-owned Rerun sink with modality-level selection and tests."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0039"
title = "Streamlit Pipeline page breaks ViSTA request replay because session-state JSON round-tripping stringifies strict Path fields"
status = "resolved"
priority = "high"
category = "app"
summary = "The pipeline TOML loads and plans correctly, but the Streamlit Pipeline page persists `slam_backend_payload` through JSON session state, converts ViSTA `Path` fields into strings, and then fails when rebuilding the strict `VistaBackendSpec` for request preview and planning."
files = [
    "src/prml_vslam/app/state.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    ".configs/pipelines/vista-full.toml",
    "tests/test_app.py",
]
notes = [
    "Direct TOML hydration through `RunRequest.from_toml()` still produces proper `Path` instances for `vista_slam_dir`, `checkpoint_path`, and `vocab_path`, so the persisted config itself is valid.",
    "The Pipeline page stores `request.slam.backend.model_dump(mode=\"python\")` in `slam_backend_payload`, then `SessionStateStore.save()` serializes the whole app state with `model_dump(mode=\"json\")`, which turns those `Path` values into strings.",
    "When `build_request_from_action()` rebuilds the request, `backend_payload_from_action()` feeds those stringified values back into the discriminated `VistaBackendSpec`, and strict transport validation rejects them as non-`Path` values.",
    "This is a real workbench regression because the page can show `Resolved Request` validation errors and fail to plan even though the same ViSTA config plans successfully outside the app path.",
]
sources = [
    "src/prml_vslam/app/state.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    ".configs/pipelines/vista-full.toml",
    "tests/test_app.py",
]
previous_status = "open"
resolved_on = "2026-04-16"
resolution_note = "Closed after normalizing strict backend Path fields during request rebuild so the Streamlit Pipeline page survives app-state JSON round-trips for ViSTA configs."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0042"
title = "Parallel SE(3) datamodels duplicate transform ownership and semantics"
status = "resolved"
priority = "medium"
category = "contracts"
summary = "The repo currently carries both `SE3Pose` and `FrameTransform`, each with overlapping quaternion normalization, matrix conversion, and TUM serialization logic, even though `FrameTransform` is already the canonical frame-labeled transform DTO."
files = [
    "src/prml_vslam/interfaces/camera.py",
    "src/prml_vslam/interfaces/transforms.py",
    "src/prml_vslam/AGENTS.md",
    "docs/architecture/interfaces-and-contracts.md",
]
notes = [
    "The duplicate conversion logic is easy to keep temporarily, but it invites semantic drift because only one of the two types carries explicit frame labels.",
    "The current package guidance already treats `FrameTransform` as the canonical pose-like contract for runtime and boundary-facing transforms.",
    "This is a smaller issue than the typed-boundary drift, but it is a clear redundancy target for future simplification work.",
]
sources = [
    "src/prml_vslam/interfaces/camera.py",
    "src/prml_vslam/interfaces/transforms.py",
    "src/prml_vslam/AGENTS.md",
    "docs/architecture/interfaces-and-contracts.md",
]
previous_status = "open"
resolved_on = "2026-04-17"
resolution_note = "Removed SE3Pose and kept FrameTransform as the sole shared pose/transform DTO."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0044"
title = "Run coordinator restart policy is unsafe without event-log restore"
status = "resolved"
priority = "high"
category = "orchestration"
summary = "The Ray run coordinator is restartable even though it still rebuilds empty in-memory state after crashes and cannot restore from the durable JSONL event log, so a restart can silently fork the semantic truth for an active run."
files = [
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/supervisor.py",
    ".agents/work/pipeline-refactor/10-agent_context_ray_pipeline.md",
    ".agents/work/pipeline-refactor/13-codex-plan.md",
]
notes = [
    "The frozen plan explicitly allowed the supervisor actor to be restartable first, but deferred coordinator restarts until checkpoint/projection restore exists.",
    "The current coordinator constructor recreates snapshot, event, handle, and stage-outcome state from scratch and `start()` seeds a fresh run instead of replaying the durable event log.",
    "A coordinator crash during an active run would therefore revive a new empty state owner behind the same logical run id and violate the event-projected truth model.",
]
sources = [
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/supervisor.py",
    ".agents/work/pipeline-refactor/10-agent_context_ray_pipeline.md",
    ".agents/work/pipeline-refactor/13-codex-plan.md",
]
previous_status = "open"
resolved_on = "2026-04-18"
resolution_note = "Coordinator restart-without-restore is closed for the active runtime path because RunCoordinatorActor is no longer restartable."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0045"
title = "Terminal stage errors still bypass the `stage.failed` event path"
status = "resolved"
priority = "high"
category = "orchestration"
summary = "The runtime defines a stage-scoped failure event, but terminal exceptions still collapse directly into `run.failed`, so failing stage outcomes, registry failure modes, and stage-level provenance are not represented in the durable event stream."
files = [
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/stage_registry.py",
    ".agents/work/pipeline-refactor/13-codex-plan.md",
]
notes = [
    "The top-level coordinator exception path still emits only `RunFailed`, and the streaming finalization path does the same when `_streaming_error` is set.",
    "That leaves the declared `StageFailed` contract unused by the active runtime even though stage keys, failure modes, and `StageOutcome` already exist.",
    "Without a durable stage-scoped terminal event, the event log cannot explain which stage failed or preserve the same provenance shape as successful stages.",
]
sources = [
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/stage_registry.py",
    ".agents/work/pipeline-refactor/13-codex-plan.md",
]
previous_status = "open"
resolved_on = "2026-04-18"
resolution_note = "Terminal stage failures are now represented by StageFailed events before run-level failure, and snapshot projection/tests reflect the stage-scoped terminal outcome."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0046"
title = "Late stop requests can still rewrite completed streaming runs to STOPPED"
status = "resolved"
priority = "medium"
category = "orchestration"
summary = "A completed streaming run can still be mutated to `STOPPED` because `stop()` appends `run.stopped` after the worker finishes and the snapshot projector applies that terminal state unconditionally."
files = [
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/snapshot_projector.py",
    "src/prml_vslam/pipeline/run_service.py",
]
notes = [
    "`RunService.start_run()` stops the previous run before launching the next one, so this is not only a manual-stop corner case.",
    "The projector correctly guards `RunStopRequested` from overwriting `COMPLETED` or `FAILED`, but `RunStopped` still forces `STOPPED` even after successful completion.",
    "That means terminal state history for streaming runs is still mutable after completion and can disagree with what actually happened.",
]
sources = [
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/snapshot_projector.py",
    "src/prml_vslam/pipeline/run_service.py",
]
previous_status = "open"
resolved_on = "2026-04-18"
resolution_note = "Late stop requests no longer rewrite completed or failed streaming runs because both coordinator and snapshot projector preserve terminal state immutability."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0047"
title = "PlacementPolicy still advertises semantics the Ray translator does not honor"
status = "resolved"
priority = "medium"
category = "architecture"
summary = "The placement contract already advertises pools, colocation, spread, and stage-owned placement keys, but the current Ray translation still consumes only resource numbers and still relies on free-form stage strings."
files = [
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/placement.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/README.md",
    ".agents/work/pipeline-refactor/13-codex-plan.md",
]
notes = [
    "The frozen refactor plan called for a repo-owned `PlacementPolicy` translated into resources and placement groups by the Ray backend only.",
    "The current translator ignores `pool`, `colocate_with`, and `spread`, and the runtime still uses ad hoc string keys such as `\"packet_source\"` instead of a typed stage-owned key set.",
    "Until either the contract shrinks or the implementation catches up, the public request shape overpromises what the backend can actually honor.",
]
sources = [
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/placement.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/README.md",
    ".agents/work/pipeline-refactor/13-codex-plan.md",
]
previous_status = "open"
resolved_on = "2026-04-18"
resolution_note = "The placement contract now matches the implemented Ray translator: typed stage keys plus resource overrides only, with no latent colocation/spread semantics."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0048"
title = "The repo-owned Rerun sink does not keep live telemetry on an explicit timeline"
status = "resolved"
priority = "medium"
category = "visualization"
summary = "The latest keyframe-visualization event now sets a keyframe sequence timeline, but later live packet and pose logs do not establish their own time context, so sticky Rerun timeline state can attach non-keyframe telemetry to stale keyframe indices."
files = [
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/methods/events.py",
    "src/prml_vslam/visualization/README.md",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
    "tests/test_streaming_visualization.py",
]
notes = [
    "The new `KeyframeVisualizationReady` event makes per-keyframe visualization ownership clearer, but it also introduced the first explicit `set_time(\"keyframe\", ...)` call in the repo-owned sink.",
    "Rerun stream time context is sticky, so later `PacketObserved` RGB logs and `PoseEstimated` camera-pose logs need their own explicit timeline semantics if they should not inherit the last keyframe sequence.",
    "The current sink tests validate entity paths and payload routing, but they do not yet cover interleaved keyframe and live telemetry on separate timelines.",
]
sources = [
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/methods/events.py",
    "src/prml_vslam/visualization/README.md",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
    "tests/test_streaming_visualization.py",
]
previous_status = "open"
resolved_on = "2026-04-18"
resolution_note = "Live Rerun telemetry now establishes explicit timeline semantics for live packet/pose logging, and the sink tests cover the stale-keyframe-timeline regression path."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0043"
title = "Leaf modules still mix rendering, orchestration, and test-only ownership"
status = "resolved"
priority = "medium"
category = "maintainability"
summary = "Several leaf modules still own responsibilities that should live in thinner controllers, shared seams, or test-only packages, which keeps small modules deceptively busy and blurs package ownership."
files = [
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/state.py",
    "src/prml_vslam/pipeline/source_resolver.py",
    "src/prml_vslam/pipeline/testing_support.py",
]
notes = [
    "The pipeline snapshot view still combines rendering with controller-like work such as tailing events, reading cached arrays, selecting plan-tab event payloads, and triggering evo-preview resolution.",
    "Session-state loading still carries a dead `required_methods` parameter that is immediately discarded, which signals an abstraction that no longer enforces what it advertises.",
    "The next ownership cut is to move snapshot-derived view data and preview resolution behind `pipeline_controller.py` so the page module becomes renderer-only.",
    "The pipeline package still ships test doubles under `src/`, and `source_resolver.py` already carries an inline TODO questioning whether its ownership is correct.",
]
sources = [
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/state.py",
    "src/prml_vslam/pipeline/source_resolver.py",
    "src/prml_vslam/pipeline/testing_support.py",
]
previous_status = "open"
resolved_on = "2026-04-18"
resolution_note = "This slice removed the most concrete current drift by moving test-only helpers out of src/ and pushing raw backend-notice shaping behind the controller helper used by the pipeline snapshot page."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0039"
title = "Pipeline page request state still needs an explicit JSON-friendly replay seam after the page split"
status = "resolved"
priority = "high"
category = "app"
summary = "The Pipeline page is now smaller, but the request replay seam is still split across page helpers, controller code, and JSON-friendly metadata bags, so strict backend-spec rebuilds are not yet controller-owned end to end."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
notes = [
    "The Pipeline page is being split into request-editor and snapshot-view helpers so the top-level page stops owning all rendering branches directly.",
    "The controller now persists the backend payload in JSON mode, which keeps the session-state boundary explicit instead of depending on mixed in-memory Python objects.",
    "The page surface still leaks replay logic because `build_streaming_source_from_action()` returns an untyped runtime object and `handle_pipeline_page_action()` still bridges page state directly into the run-service seam.",
    "The app-side request-edit path still carries partial type erasure such as `PipelinePageState.slam_backend_payload: dict[str, object]` and `previewable_statuses: list[object]` in the request editor.",
    "The remaining work is to keep the controller-owned request rebuild path and its focused tests authoritative as the page surface continues to shrink.",
]
sources = [
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "The bounded Pipeline page now keeps the JSON-friendly request-edit path but executes from one authoritative request contract, with runtime-source construction shared with the CLI through build_runtime_source_from_request."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0041"
title = "The ViSTA adapter still relies on global import-path mutation and private upstream APIs"
status = "resolved"
priority = "high"
category = "integration"
summary = "The ViSTA integration no longer mutates `sys.path`, no longer probes global site-package locations for DBoW3 shared libraries, and no longer depends on the private upstream `_crop_resize_if_necessary_image_only` helper."
files = [
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/methods/vista/config.py",
    "src/prml_vslam/methods/README.md",
    "src/prml_vslam/AGENTS.md",
]
notes = [
    "The adapter now installs an explicit `vista_slam` namespace package rooted at the checked-out upstream repo instead of mutating global `sys.path`.",
    "It imports `DBoW3Py` as a declared dependency and fails with an actionable error if that dependency is not importable, rather than probing site-packages and build directories for a shared library.",
    "The upstream frame preprocessor now uses an explicit local resize boundary instead of the private `_crop_resize_if_necessary_image_only` helper.",
    "The remaining wrapper logic still converts upstream poses and intrinsics back into `FrameTransform`, `CameraIntrinsics`, and `SlamArtifacts`, so the behavior stays aligned with the pipeline contracts while the import boundary is cleaner.",
]
sources = [
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/methods/vista/config.py",
    "src/prml_vslam/methods/README.md",
    "src/prml_vslam/AGENTS.md",
]
previous_status = "resolved"
resolved_on = "2026-04-18"
resolution_note = "The ViSTA wrapper now uses an explicit upstream namespace-package boundary, no longer mutates sys.path, no longer probes site-packages for DBoW3 shared libraries, and no longer depends on the private upstream image-preprocessing helper."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0032"
title = "Record3D Wi-Fi preview remains a large repo-owned subsystem for a preview-only path"
status = "resolved"
priority = "medium"
category = "maintainability"
summary = "USB is documented as the canonical Record3D integration, but the repo still carries a bespoke Wi-Fi signaling, WebRTC receiver, packet-decoding, and session stack for the optional preview fallback."
files = [
    "src/prml_vslam/io/wifi_session.py",
    "src/prml_vslam/io/wifi_receiver.py",
    "src/prml_vslam/io/wifi_packets.py",
    "src/prml_vslam/io/wifi_signaling.py",
    "src/prml_vslam/io/README.md",
    "README.md",
]
notes = [
    "The current Wi-Fi implementation is intentionally preview-only, lower fidelity, and missing the full pose/confidence surfaces available on USB.",
    "Even with that limited role, the path still owns custom signaling, worker, and decoding logic that materially increases the size of `io/`.",
    "This remains a relevant LOC-reduction target from the older reviews, even though the current docs already scope it correctly.",
]
sources = [
    "README.md",
    "src/prml_vslam/io/README.md",
    "src/prml_vslam/io/wifi_session.py",
    "src/prml_vslam/io/wifi_receiver.py",
    "src/prml_vslam/io/wifi_packets.py",
    "src/prml_vslam/io/wifi_signaling.py",
]
previous_status = "open"
resolved_on = "2026-04-18"
resolution_note = "Retired after user clarified that Record3D Wi-Fi is stable and equivalent to the USB path, so the preview-only/lower-fidelity premise was incorrect."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0040"
title = "Typed pipeline and method seams still degrade to object-typed boundaries"
status = "resolved"
priority = "high"
category = "architecture"
summary = "The repo already defines protocol seams and discriminated backend specs, but the main orchestration path still widens them back to `object` or `Any` across pipeline, Ray, factory, controller, and app seams, which weakens language-server guidance and makes the declared typed boundaries largely decorative."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/protocols/source.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/contracts/runtime.py",
    "src/prml_vslam/pipeline/backend.py",
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/ray_runtime/supervisor.py",
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/methods/factory.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
    "src/prml_vslam/app/pages/record3d.py",
]
notes = [
    "The repo already owns `OfflineSequenceSource`, `StreamingSequenceSource`, and the `BackendSpec` union, so most of these `object` surfaces are not expressing a genuinely open-ended contract.",
    "Recent cleanup removed several of the original drift points: `PipelineRuntimeSource` is now typed, the public snapshot no longer carries `latest_backend_event`, Streamlit now resolves runtime sources through the shared request-to-source path, and the old supervisor layer is gone.",
    "The remaining active seams are smaller but still real: `slam_backend_payload: dict[str, object]` in app state, a few `object`-typed pose/intrinsics parameters inside the repo-owned Rerun sink, and helper/test surfaces that still use broad `dict[str, object]` payloads for convenience.",
    "This is the clearest remaining anti-pattern relative to the new package guidance because it blocks static discovery of exactly the seams the codebase already declared.",
]
sources = [
    "src/prml_vslam/protocols/source.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/contracts/runtime.py",
    "src/prml_vslam/pipeline/backend.py",
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/ray_runtime/supervisor.py",
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/methods/factory.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
    "src/prml_vslam/app/pages/record3d.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "Resolved the remaining main-orchestration typed-seam debt: the Pipeline page now preserves a typed BackendSpec instead of a raw payload bag, backend-spec overrides stay typed through request and factory helpers, the Ray backend/coordinator/sink handles use concrete types, and the ADVIO pipeline-demo CLI now executes from the same request-to-runtime-source path as run-config and Streamlit."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0056"
title = "SLAM live DTOs still sit in shared interfaces while importing pipeline transport handles"
status = "resolved"
priority = "high"
category = "architecture"
summary = "`interfaces.slam` still owns `SlamUpdate`, backend notice DTOs, and handle-bearing visualization notices that import pipeline transport contracts, even though the target architecture assigns live SLAM semantics to `methods` and live payload references to pipeline runtime-update contracts."
files = [
    "src/prml_vslam/interfaces/slam.py",
    "src/prml_vslam/interfaces/__init__.py",
    "src/prml_vslam/methods/events.py",
    "src/prml_vslam/methods/protocols.py",
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/stages/slam/runtime.py",
    "src/prml_vslam/pipeline/stages/slam/visualization.py",
]
notes = [
    "The DTO migration ledger assigns `SlamUpdate`, `BackendEvent`, and backend notice variants to WP-06/WP-08/WP-10 migration work.",
    "The current import direction weakens the intended layering where `interfaces` owns stable repo-wide semantic datamodels and `pipeline` owns runtime transport envelopes.",
    "The immediate docstring requirement is to document the migration status clearly so readers do not treat `interfaces.slam` as the desired final ownership shape.",
]
sources = [
    "docs/architecture/pipeline-dto-migration-ledger.md",
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "docs/architecture/pipeline-stage-refactor-target.md",
    "src/prml_vslam/interfaces/slam.py",
]
previous_status = "open"
resolved_on = "2026-04-23"
resolution_note = "Resolved by the current WP-09C migration slice: live SLAM DTOs now live in methods.contracts, interfaces.slam exports only SlamArtifacts, and interfaces no longer imports pipeline handle contracts. Remaining live-handle cleanup is tracked under ISSUE-0063/TODO-0063."
source_collection = "issues"

[[resolved_issues]]
id = "ISSUE-0057"
title = "Run events still mix durable provenance with live telemetry migration events"
status = "resolved"
priority = "high"
category = "orchestration"
summary = "The target model makes `RunEvent` durable lifecycle/provenance truth and routes progress, packets, backend notices, visualization items, and live payload refs through `StageRuntimeUpdate`, but current contracts still expose telemetry variants and compatibility projections in the durable event vocabulary."
files = [
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/contracts/runtime.py",
    "src/prml_vslam/pipeline/snapshot_projector.py",
    "src/prml_vslam/pipeline/stages/base/contracts.py",
    "src/prml_vslam/pipeline/stages/base/handles.py",
    "src/prml_vslam/pipeline/stages/slam/runtime.py",
]
notes = [
    "`StageProgressed`, `PacketObserved`, and `BackendNoticeReceived` are marked as migration contacts in code and docs.",
    "The target `StageRuntimeUpdate` and `TransientPayloadRef` DTOs exist, but compatibility event consumers still need to be cut over before the durable/live split is clean.",
    "This debt affects public-interface docstrings because callers need to know which fields are canonical target state and which are compatibility views.",
]
sources = [
    "docs/architecture/pipeline-stage-refactor-target.md",
    "docs/architecture/pipeline-dto-migration-ledger.md",
    "docs/architecture/pipeline-refactor-work-packages/WP-08-snapshot-events-payloads.md",
    "src/prml_vslam/pipeline/contracts/events.py",
]
previous_status = "open"
resolved_on = "2026-04-23"
resolution_note = "Resolved by the current WP-09C migration slice: durable RunEvent now contains lifecycle/provenance events only, packet/backend telemetry routes through StageRuntimeUpdate, and RunSnapshot stage_status/stage_progress compatibility properties are gone. Remaining migration-only symbols are tracked under ISSUE-0063/TODO-0063 and app preview cleanup under TODO-0065."
source_collection = "issues"

[[resolved_todos]]
id = "TODO-0035"
title = "Re-triage partially addressed resolved records against the current code"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0033",
]
loc_min = 10
loc_expected = 25
loc_max = 60
files = [
    ".agents/issues.toml",
    ".agents/todos.toml",
    ".agents/resolved.toml",
]
acceptance = [
    "Resolved archive entries that are only partially addressed are either amended with narrower resolution notes or reopened as active issues.",
    "The active backlog and resolved archive no longer imply that today’s still-open findings are fully closed.",
    "`make agents-db` reflects the intended active work after the re-triage pass.",
]
sources = [
    ".agents/issues.toml",
    ".agents/todos.toml",
    ".agents/resolved.toml",
]
previous_status = "pending"
resolved_on = "2026-04-18"
resolution_note = "Closed after the active backlog was re-triaged against the current tree and stale supervisor-era wording was removed from the remaining active Ray smoke records."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0051"
title = "Inline `stage_manifest_from_outcome()` into `project_summary()`"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0049",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 3
loc_expected = 8
loc_max = 18
files = [
    "src/prml_vslam/pipeline/finalization.py",
]
acceptance = [
    "`project_summary()` constructs `StageManifest` entries directly or otherwise removes the single-use wrapper.",
    "`stage_manifest_from_outcome()` is deleted unless a second real caller or stronger abstraction need exists.",
    "The persisted stage-manifest payload remains unchanged after the simplification.",
]
sources = [
    "src/prml_vslam/pipeline/finalization.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "Closed after collapsing summary projection to the direct manifest-construction path."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0052"
title = "Move `compute_trajectory_evaluation()` behind the eval package seam"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0050",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 30
loc_expected = 70
loc_max = 140
files = [
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
]
acceptance = [
    "The evo-specific trajectory evaluation implementation lives under `prml_vslam.eval` rather than `prml_vslam.pipeline.finalization`.",
    "The pipeline stage keeps only the orchestration needed to call the evaluation seam and attach resulting artifacts.",
    "The persisted trajectory metrics payload and stage outputs remain behaviorally unchanged after the ownership move.",
]
sources = [
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "Closed after routing the pipeline trajectory-evaluation stage through the eval-owned implementation seam."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0002"
title = "Declare PyYAML explicitly for ADVIO calibration loading"
status = "resolved"
priority = "high"
issue_ids = [
    "ISSUE-0002",
]
loc_min = 1
loc_expected = 4
loc_max = 12
files = [
    "pyproject.toml",
    "src/prml_vslam/datasets/advio/advio_loading.py",
]
acceptance = [
    "PyYAML is declared in the appropriate dependency group.",
    "ADVIO calibration loading works in a clean install without transitive dependency luck.",
]
previous_status = "pending"
resolved_on = "2026-04-05"
resolution_note = "Added PyYAML to runtime dependencies and locked with a pyproject dependency test."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0001"
title = "Add TOML-backed config entrypoints for durable BaseConfig workflows"
status = "resolved"
priority = "high"
issue_ids = [
    "ISSUE-0001",
]
loc_min = 25
loc_expected = 60
loc_max = 120
files = [
    "src/prml_vslam/main.py",
    "README.md",
]
acceptance = [
    "At least one durable CLI workflow accepts a repo-owned .toml config path.",
    "The implementation uses BaseConfig.from_toml() and PathConfig.resolve_toml_path().",
    "README or CLI help documents the TOML-first configuration path.",
]
previous_status = "pending"
resolved_on = "2026-04-05"
resolution_note = "Implemented plan-run-config TOML entrypoint and documented TOML-first run planning in README."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0012"
title = "Align ADVIO app downloads with the empty-selection means all-scenes contract"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0012",
]
loc_min = 8
loc_expected = 20
loc_max = 50
files = [
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/app/pages/advio.py",
    "tests/test_app.py",
]
acceptance = [
    "The app either honors `sequence_ids=[]` as 'all scenes' or the shared ADVIO request contract is changed consistently across app, CLI, and dataset code.",
    "UI messaging no longer contradicts the shared request semantics.",
    "Tests cover the chosen empty-selection behavior.",
]
sources = [
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/main.py",
    "tests/test_app.py",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15#discussion_r3037245906",
]
previous_status = "pending"
resolved_on = "2026-04-05"
resolution_note = "The app now treats an empty ADVIO scene selection as the full catalog and documents that behavior in the form UI."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0006"
title = "Remove placeholder config and state fields that no longer earn their keep"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0006",
]
loc_min = 25
loc_expected = 45
loc_max = 90
files = [
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/methods/mock_vslam.py",
    "README.md",
    "tests/test_main.py",
    "tests/test_methods.py",
]
acceptance = [
    "Source-spec config loading no longer depends on explicit `kind` discriminator fields.",
    "The USB Record3D packet-stream config no longer wraps the low-level stream config through a one-field `stream` indirection.",
    "Mock SLAM backend config no longer validates unused `method_id` state.",
    "TOML examples and tests match the simplified config surfaces.",
]
sources = [
    "README.md",
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/methods/mock_vslam.py",
    "tests/test_main.py",
    "tests/test_methods.py",
]
previous_status = "pending"
resolved_on = "2026-04-05"
resolution_note = "Updated the contracts, TOML example, and tests to the simplified config surfaces; full make ci passed."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0015"
title = "Factor SessionStateStore runtime loading through one generic helper"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0007",
    "ISSUE-0015",
]
loc_min = 25
loc_expected = 55
loc_max = 120
files = [
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
acceptance = [
    "One shared helper handles session-state lookup, compatibility validation, stale-runtime shutdown, and replacement.",
    "Record3D, ADVIO, and Pipeline runtime loads configure that helper instead of reimplementing the same control flow.",
    "Focused tests cover the preserved stale-runtime replacement behavior after the refactor.",
]
sources = [
    "src/prml_vslam/app/REQUIREMENTS.md",
    "src/prml_vslam/app/AGENTS.md",
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
previous_status = "pending"
resolved_on = "2026-04-05"
resolution_note = "Factored SessionStateStore runtime loading through a shared helper and added focused stale-runtime replacement coverage in tests/test_app.py."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0007"
title = "Add focused tests for stale runtime replacement in SessionStateStore"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0007",
]
loc_min = 25
loc_expected = 60
loc_max = 120
files = [
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
acceptance = [
    "Tests cover incompatible runtime replacement for record3d, advio, and pipeline adapters.",
    "Tests assert stale runtimes are shut down exactly once before replacement.",
]
previous_status = "pending"
resolved_on = "2026-04-05"
resolution_note = "Added focused SessionStateStore replacement tests for record3d, ADVIO, and pipeline runtimes, asserting stale runtimes are stopped exactly once before replacement."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0015"
title = "Factor SessionStateStore runtime loading through one generic helper"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0007",
    "ISSUE-0015",
]
loc_min = 25
loc_expected = 55
loc_max = 120
files = [
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
acceptance = [
    "One shared helper handles session-state lookup, compatibility validation, stale-runtime shutdown, and replacement.",
    "Record3D, ADVIO, and Pipeline runtime loads configure that helper instead of reimplementing the same control flow.",
    "Focused tests cover the preserved stale-runtime replacement behavior after the refactor.",
]
sources = [
    "src/prml_vslam/app/REQUIREMENTS.md",
    "src/prml_vslam/app/AGENTS.md",
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
previous_status = "pending"
resolved_on = "2026-04-05"
resolution_note = "Factored SessionStateStore runtime loading through a shared helper and added focused stale-runtime replacement coverage in tests/test_app.py."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0007"
title = "Add focused tests for stale runtime replacement in SessionStateStore"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0007",
]
loc_min = 25
loc_expected = 60
loc_max = 120
files = [
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
acceptance = [
    "Tests cover incompatible runtime replacement for record3d, advio, and pipeline adapters.",
    "Tests assert stale runtimes are shut down exactly once before replacement.",
]
previous_status = "pending"
resolved_on = "2026-04-05"
resolution_note = "Added focused SessionStateStore replacement tests for record3d, ADVIO, and pipeline runtimes, asserting stale runtimes are stopped exactly once before replacement."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0003"
title = "Move live preview snapshot contracts into app-owned models"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0003",
]
loc_min = 30
loc_expected = 95
loc_max = 180
files = [
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pages/record3d.py",
    "tests/test_app.py",
]
acceptance = [
    "Record3D transport code exposes only packet and transport-facing contracts.",
    "Record3D and ADVIO preview snapshot models live under `prml_vslam.app` instead of `io.record3d` or `app.services`.",
    "App pages, runtime controllers, and tests no longer depend on preview snapshot types owned by transport or service modules.",
]
sources = [
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/models.py",
    "tests/test_app.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Preview snapshot contracts now live under app models and app modules import them from there."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0004"
title = "Consolidate app page-state save-if-changed helpers"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0004",
]
loc_min = 20
loc_expected = 45
loc_max = 90
files = [
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/record3d_controller.py",
]
acceptance = [
    "One shared helper handles typed page-state mutation and persistence.",
    "ADVIO, Pipeline, and Record3D controllers stop duplicating the same save-if-changed pattern.",
    "Existing app tests still cover the preserved behavior.",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "The shared save_model_updates helper now owns the app page-state persistence path without the old per-surface wrappers."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0005"
title = "Split ingress packets from preview-only SLAM and confidence payloads"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0005",
]
loc_min = 40
loc_expected = 125
loc_max = 240
files = [
    "src/prml_vslam/interfaces/runtime.py",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/methods/mock_vslam.py",
    "src/prml_vslam/app/pages/record3d.py",
    "src/prml_vslam/app/pages/pipeline.py",
]
acceptance = [
    "Unused `FramePacket` fields are removed or justified by an active ingress caller.",
    "Sensor confidence is no longer exposed as `FramePacket.uncertainty` when `SlamUpdate.uncertainty` is method-oriented output.",
    "Pointmap ownership is narrowed so ingress packets and SLAM updates do not both claim the same preview payload without a clear contract boundary.",
]
sources = [
    "README.md",
    "docs/Questions.md",
    "src/prml_vslam/app/REQUIREMENTS.md",
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/interfaces/runtime.py",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/pipeline/contracts.py",
    "src/prml_vslam/methods/mock_vslam.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Ingress packets no longer duplicate pointmap ownership and the Record3D confidence raster uses a dedicated confidence field."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0009"
title = "Remove or privatize MethodRunResult as a parallel normalized-output surface"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0009",
]
loc_min = 35
loc_expected = 90
loc_max = 190
files = [
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/methods/base.py",
    "tests/test_methods.py",
]
acceptance = [
    "Method wrappers no longer expose a public normalized-output contract that duplicates pipeline-owned artifacts.",
    "Any remaining wrapper planning object is clearly private to the methods layer or maps directly onto pipeline-owned outputs.",
    "Method tests assert the new ownership boundary rather than the old `MethodRunResult` shape.",
]
sources = [
    "README.md",
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/methods/base.py",
    "tests/test_methods.py",
    "src/prml_vslam/methods/README.md",
    "https://github.com/zhangganlin/vista-slam",
    "https://github.com/rmurai0610/MASt3R-SLAM",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "The public method mock surface now takes raw paths and returns pipeline-owned SlamArtifacts rather than a separate normalized-output DTO."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0010"
title = "Trim io and pipeline package exports to the documented minimal surface"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0010",
]
loc_min = 15
loc_expected = 40
loc_max = 90
files = [
    "src/prml_vslam/io/__init__.py",
    "src/prml_vslam/pipeline/__init__.py",
]
acceptance = [
    "The package roots stop re-exporting transport internals, snapshot types, and signaling helpers that are not part of the minimal public API.",
    "Imports in the repo become explicit module imports when they need package-private helpers.",
    "The remaining package-root exports match the architecture guide and `AGENTS.md` policy.",
]
sources = [
    "docs/architecture/interfaces-and-contracts.md",
    "src/prml_vslam/AGENTS.md",
    "src/prml_vslam/io/__init__.py",
    "src/prml_vslam/pipeline/__init__.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Minimal package-root exports are now documented and verified by tests so transport internals and session helpers stay module-local."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0011"
title = "Delegate ADVIO archive fetching mechanics to a dataset fetch helper"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0011",
]
loc_min = 35
loc_expected = 90
loc_max = 180
files = [
    "src/prml_vslam/datasets/advio/advio_download.py",
    "pyproject.toml",
    "tests/test_advio.py",
]
acceptance = [
    "Archive caching, checksum verification, and download retries are handled by a dedicated helper such as Pooch rather than bespoke HTTP glue.",
    "The ADVIO catalog remains the source of truth for sequence metadata and hashes.",
    "Existing higher-level dataset service behavior stays compatible for the app and tests.",
]
sources = [
    "src/prml_vslam/datasets/README.md",
    "src/prml_vslam/datasets/advio/advio_download.py",
    "pyproject.toml",
    "https://github.com/AaltoVision/ADVIO",
    "https://zenodo.org/record/1320824",
    "https://www.fatiando.org/pooch/latest/",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15#discussion_r3037245887",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "ADVIO download now delegates cache validation and retrieval to DatasetFetchHelper, and tests cover refreshing a corrupted cached archive."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0013"
title = "Clean up stale Record3D docs references and source-of-truth placeholders"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0013",
]
loc_min = 6
loc_expected = 18
loc_max = 45
files = [
    "src/prml_vslam/io/README.md",
    "src/prml_vslam/app/README.qmd",
    "src/prml_vslam/app/AGENTS.md",
    "docs/Questions.md",
]
acceptance = [
    "Record3D docs use the current `FramePacket` contract name instead of `Record3DFramePacket`.",
    "The markdown link in `app/AGENTS.md` resolves correctly on GitHub.",
    "The ARCore placeholder TODO in `docs/Questions.md` is replaced with actual wording or removed.",
]
sources = [
    "src/prml_vslam/io/README.md",
    "src/prml_vslam/app/README.qmd",
    "src/prml_vslam/app/AGENTS.md",
    "docs/Questions.md",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15#discussion_r3036947625",
    "https://github.com/JanDuchscherer104/prml-vslam/pull/15#discussion_r3037245891",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "The stale Record3D packet references, broken local markdown link, and placeholder questions text are now replaced with current repo terminology and concrete wording."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0014"
title = "Narrow the Record3D Wi-Fi/WebRTC path behind an explicit optional boundary"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0014",
]
loc_min = 40
loc_expected = 110
loc_max = 220
files = [
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/io/wifi_session.py",
    "src/prml_vslam/io/wifi_signaling.py",
    "src/prml_vslam/io/wifi_receiver.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/pages/record3d.py",
    "README.md",
]
acceptance = [
    "The official `record3d` USB bindings remain the canonical programmatic ingress path for Record3D capture.",
    "The custom Wi-Fi signaling and receiver flow is either hidden behind a clearly optional/private adapter boundary or explicitly documented as a separate supported integration surface.",
    "README and app-facing transport semantics make the Wi-Fi support level clear instead of treating it as an unqualified peer of the official USB path.",
]
sources = [
    "README.md",
    "docs/Questions.md",
    "src/prml_vslam/io/record3d.py",
    "src/prml_vslam/io/wifi_session.py",
    "src/prml_vslam/io/wifi_signaling.py",
    "src/prml_vslam/io/wifi_receiver.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/pages/record3d.py",
    "https://record3d.app/",
    "https://github.com/marek-simonik/record3d",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "The custom Wi-Fi/WebRTC path is now positioned as an optional preview transport with explicit lower-fidelity guarantees instead of a co-equal capture boundary."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0015"
title = "Consolidate repeated trajectory plotting internals"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0015",
]
loc_min = 25
loc_expected = 60
loc_max = 130
files = [
    "src/prml_vslam/plotting/metrics.py",
    "src/prml_vslam/plotting/record3d.py",
    "src/prml_vslam/plotting/trajectories.py",
    "src/prml_vslam/plotting/pipeline.py",
]
acceptance = [
    "One internal helper or builder owns the repeated trajectory trace and layout assembly used across plotting modules.",
    "Page-specific plots still keep their unique behavior such as APE coloring or live hover text without duplicating the common trajectory scaffolding.",
    "The remaining public plotting functions stay small and page-oriented instead of reimplementing base trajectory rendering each time.",
]
sources = [
    "src/prml_vslam/plotting/metrics.py",
    "src/prml_vslam/plotting/record3d.py",
    "src/prml_vslam/plotting/trajectories.py",
    "src/prml_vslam/plotting/pipeline.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Shared internal trajectory helpers now own the repeated Plotly trace and layout assembly, and plotting tests cover the consolidated behavior."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0016"
title = "Remove packet-session and Record3D preview boilerplate"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0016",
]
loc_min = 10
loc_expected = 25
loc_max = 45
files = [
    "src/prml_vslam/utils/packet_session.py",
    "src/prml_vslam/app/services.py",
]
acceptance = [
    "Packet-session snapshot defaults no longer depend on standalone empty-array wrapper functions.",
    "The Record3D worker no longer needs a mutable holder dict or an extra helper function just to publish its connected source label.",
    "Record3D and ADVIO runtime behavior remains unchanged under the existing app and packet-stream tests.",
]
sources = [
    "src/prml_vslam/utils/packet_session.py",
    "src/prml_vslam/app/services.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Kept trial 01 after reducing LOC while preserving app and packet-stream behavior under the frozen harness."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0017"
title = "Unify app preview lifecycle states under one enum"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0017",
]
loc_min = 15
loc_expected = 30
loc_max = 60
files = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/record3d_controller.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/record3d.py",
    "tests/test_app.py",
]
acceptance = [
    "One app-owned enum defines the preview lifecycle shared by the ADVIO and Record3D runtime snapshots.",
    "Controllers, services, pages, and tests stop importing parallel enum names for the same state machine.",
    "The existing app tests continue to pass without changing runtime behavior.",
]
sources = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/record3d_controller.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/record3d.py",
    "tests/test_app.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Kept trial 02 after reducing LOC while preserving app controller and page behavior under tests/test_app.py."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0018"
title = "Inline evo TUM imports in geometry"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0018",
]
loc_min = 5
loc_expected = 12
loc_max = 25
files = [
    "src/prml_vslam/utils/geometry.py",
]
acceptance = [
    "The geometry module imports the evo TUM file adapter directly at module load instead of routing through a local helper.",
    "TUM trajectory read/write behavior stays unchanged under the existing geometry and method tests.",
    "The simplification reduces source LOC or keeps it flat while removing indirection.",
]
sources = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Kept trial 03 after reducing LOC and preserving TUM trajectory behavior under geometry, method, pipeline, and app tests."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0019"
title = "Delegate point transforms to pytransform3d"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0019",
]
loc_min = 4
loc_expected = 10
loc_max = 20
files = [
    "src/prml_vslam/utils/geometry.py",
]
acceptance = [
    "The world-frame point-transform helper uses the existing pytransform3d transform primitive instead of manually building homogeneous points.",
    "Geometry and mock-method tests continue to pass.",
    "The change reduces or keeps flat the Python LOC baseline while simplifying the helper.",
]
sources = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Kept trial 04 after holding code LOC flat, reducing total LOC, and preserving geometry/method behavior."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0020"
title = "Centralize shared app preview snapshot state"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0020",
]
loc_min = 6
loc_expected = 14
loc_max = 25
files = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/record3d_controller.py",
]
acceptance = [
    "One app-owned preview snapshot base defines the shared `state` field.",
    "One app-owned constant defines the active preview states used by both controllers.",
    "The app test harness continues to pass without changing preview behavior.",
]
sources = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/advio_controller.py",
    "src/prml_vslam/app/record3d_controller.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Kept trial 05 after reducing LOC and preserving app behavior under tests/test_app.py."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0021"
title = "Retire stale geometry TODO markers"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0021",
]
loc_min = 2
loc_expected = 4
loc_max = 8
files = [
    "src/prml_vslam/utils/geometry.py",
]
acceptance = [
    "The remaining geometry TODO markers are removed once their underlying debt has been validated as stale.",
    "Geometry and mock-method tests continue to pass.",
    "The TODO-marker count reported by `make loc LOC_ARGS=\"--todo\"` decreases.",
]
sources = [
    "src/prml_vslam/utils/geometry.py",
    "pyproject.toml",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Kept trial 06 after reducing total LOC, reducing TODO markers to zero, and preserving geometry/method behavior."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0022"
title = "Collapse mock method configs into one typed config"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0022",
]
loc_min = 20
loc_expected = 30
loc_max = 45
files = [
    "src/prml_vslam/methods/__init__.py",
    "src/prml_vslam/methods/base.py",
    "tests/test_methods.py",
]
acceptance = [
    "The methods package exposes one typed mock config plus `MethodId` instead of one-line per-backend config subclasses.",
    "Mock method setup still works through `BaseConfig.setup_target()` under the existing method and config tests.",
    "The change reduces Python LOC while preserving the current deterministic mock-artifact behavior.",
]
sources = [
    "src/prml_vslam/methods/__init__.py",
    "src/prml_vslam/methods/base.py",
    "tests/test_methods.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Kept trial 07 after reducing LOC and preserving mock-method behavior under the frozen harness plus full make ci."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0023"
title = "Data-drive app page registration and retire inject_styles"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0023",
]
loc_min = 15
loc_expected = 24
loc_max = 40
files = [
    "src/prml_vslam/app/bootstrap.py",
    "src/prml_vslam/app/ui.py",
    "tests/test_app.py",
]
acceptance = [
    "App page registration uses one small registry plus one generic entry helper instead of four page-specific wrappers.",
    "The dead `inject_styles()` seam is removed from the bootstrap path.",
    "The focused app-entry tests and full `make ci` continue to pass while Python LOC decreases.",
]
sources = [
    "src/prml_vslam/app/bootstrap.py",
    "src/prml_vslam/app/ui.py",
    "tests/test_app.py",
]
previous_status = "pending"
resolved_on = "2026-04-06"
resolution_note = "Kept trial 08 after reducing LOC and preserving bootstrap navigation behavior under focused app tests plus full make ci."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0024"
title = "Move pose-source labels onto AdvioPoseSource"
status = "resolved"
priority = "low"
issue_ids = [
    "ISSUE-0024",
]
loc_min = 6
loc_expected = 12
loc_max = 20
files = [
    "src/prml_vslam/datasets/advio/advio_models.py",
    "src/prml_vslam/app/pages/advio.py",
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/metrics.py",
]

[[resolved_todos]]
id = "TODO-0050"
title = "Give repo-owned Rerun live telemetry explicit time semantics"
status = "resolved"
priority = "medium"
issue_ids = [
    "ISSUE-0048",
]
loc_min = 20
loc_expected = 55
loc_max = 140
files = [
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/visualization/README.md",
    "tests/test_streaming_visualization.py",
]
acceptance = [
    "Live packet and pose logs establish their own timeline semantics instead of implicitly inheriting the last keyframe sequence.",
    "Keyframe visualization logs remain scoped to the keyframe timeline without tainting later live telemetry.",
    "Focused sink tests cover interleaved keyframe, live RGB, and live pose logging on explicit timelines.",
]
sources = [
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/visualization/README.md",
    "tests/test_streaming_visualization.py",
]
previous_status = "pending"
resolved_on = "2026-04-18"
resolution_note = "The repo-owned Rerun sink now clears stale keyframe timeline state for live telemetry and retains keyframe depth logging with focused sink tests."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0041"
title = "Keep the split Pipeline page on a controller-owned JSON replay path"
status = "resolved"
priority = "high"
issue_ids = [
    "ISSUE-0039",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 20
loc_expected = 55
loc_max = 130
files = [
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
acceptance = [
    "The top-level Pipeline page remains a thin coordinator over dedicated request-editor and snapshot-view helpers instead of collapsing back into one large page module.",
    "The page-state payload for ViSTA backend overrides stays JSON-friendly across app persistence and rebuilds back into a strict backend spec through the controller seam.",
    "Runtime-source construction for the bounded streaming demo also stays controller-owned instead of leaking back into page-local glue or mixed object payloads.",
    "Focused tests cover the template-sync plus request-rebuild path so the ViSTA replay surface does not silently regress.",
]
sources = [
    "src/prml_vslam/app/pages/pipeline.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/state.py",
    "tests/test_app.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "The Pipeline page now rebuilds one authoritative RunRequest including ADVIO replay controls, and both app and CLI resolve runtime sources through build_runtime_source_from_request instead of separate page-local source assembly."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0043"
title = "Make the ViSTA wrapper import and dependency boundary explicit"
status = "resolved"
priority = "high"
issue_ids = [
    "ISSUE-0041",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 70
loc_expected = 150
loc_max = 320
files = [
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/methods/vista/config.py",
    "src/prml_vslam/methods/README.md",
    "src/prml_vslam/methods/vista/README.md",
]
acceptance = [
    "The ViSTA adapter no longer mutates `sys.path` at runtime just to make the upstream checkout importable.",
    "Wrapper setup documents and isolates external prerequisites such as DBoW3 availability instead of probing multiple global locations with ad hoc runtime logic.",
    "The integration avoids the private upstream image-preprocessing helper where a stable explicit boundary can be defined or documented in the wrapper-owned setup path.",
    "The remaining wrapper logic stays thin: validate prerequisites, import explicit upstream entry points, and normalize outputs back into repo-owned `FrameTransform`, `CameraIntrinsics`, and `SlamArtifacts` contracts.",
]
sources = [
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/methods/vista/config.py",
    "src/prml_vslam/methods/README.md",
    "src/prml_vslam/AGENTS.md",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "Replaced the ViSTA wrapper\\s sys.path mutation and site-package shared-library probing with an explicit namespace-package loader, direct DBoW3Py import requirement, and a non-private frame-preprocessing boundary."
source_collection = "todos"

[[resolved_todos]]
id = "TODO-0042"
title = "Replace object-typed orchestration seams with the repo-owned protocols and unions"
status = "resolved"
priority = "high"
issue_ids = [
    "ISSUE-0040",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 110
loc_expected = 260
loc_max = 460
files = [
    "src/prml_vslam/protocols/source.py",
    "src/prml_vslam/pipeline/contracts/runtime.py",
    "src/prml_vslam/pipeline/backend.py",
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/ray_runtime/supervisor.py",
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/methods/factory.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
]
acceptance = [
    "Pipeline and app seams stop using `object` for sources, backends, and run-service dependencies where the repo already owns a concrete protocol or discriminated union.",
    "The Ray control plane uses concrete types for coordinator handles, events, and packet sources, leaving only narrowly justified `object` seams where external library payloads are still intentionally generic.",
    "The public runtime snapshot stops exposing ad hoc/raw telemetry such as `latest_backend_event: dict[str, object] | None` while retaining the explicitly approved trajectory history fields.",
    "The backend factory accepts `BackendSpec` and returns the repo-owned SLAM backend protocol instead of erasing both sides to `object`.",
    "The app/controller path uses the shared request-to-runtime-source resolver instead of a separate page-local streaming source builder, and no longer depends on `previewable_statuses: list[object]`.",
    "The language server can follow backend-spec, source, and run-service surfaces end-to-end without dynamic type erasure at the main orchestration boundaries.",
]
sources = [
    "src/prml_vslam/protocols/source.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/contracts/runtime.py",
    "src/prml_vslam/pipeline/backend.py",
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/ray_runtime/supervisor.py",
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/methods/factory.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pages/pipeline_snapshot_view.py",
]
previous_status = "in_progress"
resolved_on = "2026-04-18"
resolution_note = "Closed after replacing the remaining repo-owned object-typed orchestration seams with concrete backend-spec, JSON, Ray-handle, and event-sink types; the remaining generic payloads are either external-library adapter boundaries or view-only helpers outside the pipeline orchestration seam."
source_collection = "todos"

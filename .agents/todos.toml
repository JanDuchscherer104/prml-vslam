[[todos]]
id = "TODO-0032"
title = "Extend the evo adapter with explicit alignment and drift evaluation"
status = "in_progress"
priority = "medium"
issue_ids = [
    "ISSUE-0030",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 45
loc_expected = 115
loc_max = 240
files = [
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/eval/contracts.py",
    "src/prml_vslam/app/pages/metrics.py",
    "src/prml_vslam/eval/README.md",
]
acceptance = [
    "Persisted trajectory metrics include explicit evaluation semantics such as alignment mode, metric id, and sync tolerance.",
    "The repo can compute drift-oriented RPE alongside or instead of translation-only APE through the existing thin evo seam.",
    "The metrics page continues to keep evaluation explicit and can render the richer persisted result shape.",
]
sources = [
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/eval/contracts.py",
    "src/prml_vslam/app/pages/metrics.py",
    "README.md",
]

[[todos]]
id = "TODO-0036"
title = "Harden the real Ray end-to-end smoke path after the coordinator-only backend cutover"
status = "in_progress"
priority = "high"
issue_ids = [
    "ISSUE-0034",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 90
loc_expected = 200
loc_max = 420
files = [
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/pipeline/README.md",
    "tests/test_pipeline.py",
]
acceptance = [
    "`RayPipelineBackend` owns coordinator lifecycle directly, and deterministic tests cover that submission/readback path.",
    "The remaining real offline and streaming Ray smoke tests pass under a bounded timeout without hanging inside `ray.get(...)` or worker startup.",
    "The Ray smoke coverage is no longer opt-in-only for everyday regression detection, or a dedicated always-on CI lane covers it explicitly.",
]
sources = [
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/run_service.py",
    "tests/test_pipeline.py",
]

[[todos]]
id = "TODO-0070"
title = "WP-C — Implement real remote stage runtime support via RayStageRuntimeHandle"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0066",
]
loc_min = 90
loc_expected = 180
loc_max = 400
files = [
    "src/prml_vslam/pipeline/stages/base/proxy.py",
    "src/prml_vslam/pipeline/runtime_manager.py",
]
acceptance = [
    "RayStageRuntimeHandle is implemented and supports remote actor invocation.",
    "RuntimeManager can construct either local or remote runtime handles based on config.",
    "Smoke test verifies one stage running on a remote Ray actor with status reporting.",
]
sources = [
    ".agents/work/post-wp10-simplification/031-review-gpt-5.5pro.md",
    ".agents/work/post-wp10-simplification/032-review-gpt-5.5pro.md",
]

[[todos]]
id = "TODO-0071"
title = "WP-D — Shrink RunCoordinatorActor by extracting collaborators"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0068",
]
loc_min = 200
loc_expected = 450
loc_max = 800
files = [
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/event_log.py",
    "src/prml_vslam/pipeline/ray_runtime/payload_store.py",
]
acceptance = [
    "Coordinator size is reduced by at least 300 LOC.",
    "EventLog, PayloadStore, and ObserverRouter are extracted into testable collaborators.",
    "Pipeline execution remains correct across all modes.",
]
sources = [
    ".agents/work/post-wp10-simplification/031-review-gpt-5.5pro.md",
    ".agents/work/post-wp10-simplification/032-review-gpt-5.5pro.md",
]

[[todos]]
id = "TODO-0072"
title = "WP-E — Simplify Rerun policy with structured roles and intent-based dispatch"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0067",
]
loc_min = 120
loc_expected = 280
loc_max = 500
files = [
    "src/prml_vslam/visualization/rerun_policy.py",
    "src/prml_vslam/visualization/contracts.py",
]
acceptance = [
    "VisualizationRole is introduced to replace string constants.",
    "RerunLoggingPolicy size is reduced and it no longer has stage-specific branches.",
    "All existing visualization modalities (RGB, Depth, Points, Trajectory) still function correctly.",
]
sources = [
    ".agents/work/post-wp10-simplification/031-review-gpt-5.5pro.md",
    ".agents/work/post-wp10-simplification/032-review-gpt-5.5pro.md",
]

[[todos]]
id = "TODO-0073"
title = "WP-F — Decompose target architecture doc into package-local REQUIREMENTS.md"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0064",
]
loc_min = 40
loc_expected = 100
loc_max = 200
files = [
    "docs/architecture/pipeline-stage-refactor-target.md",
    "src/prml_vslam/*/REQUIREMENTS.md",
]
acceptance = [
    "Authoritative rules are moved to domain package REQUIREMENTS.md files.",
    "The target doc is marked as historical.",
    "Grep check confirms no stale terms (e.g., StageCatalog) in live REQUIREMENTS docs.",
]
sources = [
    ".agents/work/post-wp10-simplification/031-review-gpt-5.5pro.md",
    ".agents/work/post-wp10-simplification/032-review-gpt-5.5pro.md",
]

[[todos]]
id = "TODO-0074"
title = "Constrain StageRuntimeUpdate semantic events to a minimal live event contract"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0070",
]
loc_min = 20
loc_expected = 60
loc_max = 140
files = [
    "src/prml_vslam/pipeline/stages/base/contracts.py",
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/interfaces/alignment.py",
    "src/prml_vslam/visualization/rerun_policy.py",
    "src/prml_vslam/visualization/rerun_sink.py",
    "tests/test_pipeline_stage_contracts.py",
    "tests/test_slam_stage_runtime.py",
]
acceptance = [
    "StageRuntimeUpdate.semantic_events no longer accepts arbitrary BaseData.",
    "SLAM updates and ground-alignment metadata still cross the live-update boundary with explicit event-kind semantics.",
    "Rerun policy and sink tests cover the new event contract without adding a pipeline-owned semantic wrapper for every domain payload.",
]
sources = [
    ".agents/work/post-wp10-simplification/031-review-gpt-5.5pro.md",
    "src/prml_vslam/pipeline/stages/base/contracts.py",
    "src/prml_vslam/visualization/rerun_policy.py",
    "src/prml_vslam/visualization/rerun_sink.py",
]

[[todos]]
id = "TODO-0075"
title = "Simplify artifact cleanup to support filesystem glob patterns"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0065",
]
loc_min = 40
loc_expected = 80
loc_max = 150
files = [
    "docs/architecture/pipeline-stage-artifact-cleanup-policy.md",
    "src/prml_vslam/pipeline/stages/base/config.py",
    "tests/test_pipeline_config.py",
]
acceptance = [
    "StageConfig uses cleanup_patterns instead of cleanup_artifact_keys.",
    "Validation allows valid filesystem globs but strictly rejects absolute paths and directory traversal (..).",
    "Architecture doc and tests are updated to reflect the new glob-based policy.",
    "Existing pipeline configs (e.g., vista-full.toml) use cleanup_patterns.",
]
sources = [
    "docs/architecture/pipeline-stage-artifact-cleanup-policy.md",
    "src/prml_vslam/pipeline/stages/base/config.py",
]

[[todos]]
id = "TODO-0080"
title = "Reconcile live Rerun routing with sink throughput and reference entity fanout"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0074",
]
loc_min = 70
loc_expected = 180
loc_max = 380
files = [
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/visualization/rerun_sink.py",
    "src/prml_vslam/visualization/rerun_policy.py",
    ".configs/pipelines/vista-full.toml",
    "tests/test_pipeline.py",
    "tests/test_rerun_follow.py",
    "tests/test_visualization.py",
]
acceptance = [
    "Live Rerun routing preserves the `fix/rerun-pipeline` anti-starvation rule for heavy artifacts while still showing lightweight source references and the expected live SLAM telemetry.",
    "Selective live/export routing stays explicit: live receives runtime telemetry, model/keyframe streaming updates, frusta, and source-reference trajectories; export receives those plus heavy reference clouds and terminal artifact visualizations.",
    "Long reference trajectories do not create tens of thousands of default-visible pose-axis child entities in the online viewer unless explicitly requested.",
    "Sidecar lag warnings remain actionable: high-frequency live updates are either bounded, coalesced, or measured by tests/log assertions so a completed pipeline does not leave the viewer apparently ingesting forever.",
    "A 500-frame ADVIO-20 streaming smoke closes without repeated Rerun gRPC flush timeouts or coordinator graceful-shutdown timeout after terminal stage completion.",
    "Export-side Rerun update and close failures preserve evidence: either they fail the export path explicitly or produce a tested diagnostic artifact instead of being silently downgraded to best-effort warnings.",
    "Regression coverage proves that source references, live camera frusta, model/keyframe point clouds, and 2D image/depth views arrive at their intended live/export destinations.",
]
sources = [
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/visualization/rerun_sink.py",
    "src/prml_vslam/visualization/rerun_policy.py",
    ".configs/pipelines/vista-full.toml",
    ".logs/runs/vista-full-tuning/2026-05-31_13:49:31_vista-full-tuning.log",
    ".logs/runs/vista-full-tuning/2026-05-31_15:10:28_vista-full-tuning.log",
    ".logs/runs/vista-full-tuning/2026-05-31_16:54:44_vista-full-tuning.log",
    ".artifacts/vista-full-tuning/vista/visualization/validation/summary.json",
]

[[todos]]
id = "TODO-0083"
title = "Define and implement offline Rerun keyframe export semantics"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0077",
]
loc_min = 45
loc_expected = 120
loc_max = 260
files = [
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/methods/stage/runtime.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "tests/test_streaming_visualization.py",
    "tests/test_pipeline.py",
]
acceptance = [
    "Offline ViSTA export behavior is explicit: either persisted RRDs include bounded keyframe frusta/pointmaps/model camera updates, or tests/docs assert terminal-artifact-only export.",
    "If offline keyframe export is enabled, generated `VistaSlamRuntime` `SlamUpdate` payloads are drained from the offline `run_observations()` path into `StageRuntimeUpdate` visualizations without unbounded live sink fanout.",
    "A regression test inspects an offline exported RRD or policy-level recording and proves whether `/world/slam/vista_slam_world/keyframes/**` is expected to exist.",
    "The implementation does not confuse offline replay/export semantics with streaming live sidecar routing; any offline replay of keyframe updates targets export unless explicitly configured otherwise.",
    "The fix does not reintroduce live sidecar starvation for streaming runs.",
]
sources = [
    ".configs/pipelines/vista-full.toml",
    ".logs/runs/vista-full-tuning/2026-05-31_17:30:35_vista-full-tuning.log",
    ".artifacts/vista-full-tuning/vista/visualization/viewer_recording.rrd",
    "src/prml_vslam/methods/vista/session.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/methods/stage/runtime.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
]

[[todos]]
id = "TODO-0087"
title = "Clarify ADVIO optional provider alignment failure diagnostics"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0081",
]
loc_min = 20
loc_expected = 60
loc_max = 140
files = [
    "src/prml_vslam/sources/datasets/advio/advio_sequence.py",
    "src/prml_vslam/sources/datasets/advio/advio_geometry.py",
    "src/prml_vslam/sources/contracts.py",
    "tests/test_advio.py",
]
acceptance = [
    "If optional provider source-native output succeeds but aligned output fails, diagnostics distinguish partial success from full skip.",
    "Benchmark inputs and metadata record why `arkit_aligned_to_gt.tum` is absent for ADVIO-20.",
    "Tests cover source-native-retained/aligned-dropped ARKit behavior without weakening rotation-projection validity policy by accident.",
    "The warning path in `_append_optional_reference_trajectory()` no longer reports a complete optional-provider skip when a source-native trajectory was successfully retained.",
    "Docs or README state whether ARKit aligned output is best-effort or required for sequences with ARKit CSV.",
]
sources = [
    ".logs/runs/vista-full-tuning/2026-05-31_17:30:35_vista-full-tuning.log",
    ".artifacts/vista-full-tuning/vista/benchmark/inputs.json",
    ".artifacts/vista-full-tuning/vista/benchmark/arkit.metadata.json",
    "src/prml_vslam/sources/datasets/advio/advio_sequence.py",
    "src/prml_vslam/sources/datasets/advio/advio_geometry.py",
]

[[todos]]
id = "TODO-0092"
title = "Define offline ViSTA dense-cloud coverage semantics relative to streaming pointmaps"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0086",
]
loc_min = 35
loc_expected = 100
loc_max = 240
files = [
    "external/vista-slam/vista_slam/slam.py",
    "src/prml_vslam/methods/vista/session.py",
    "src/prml_vslam/methods/vista/artifacts.py",
    "src/prml_vslam/methods/stage/visualization.py",
    "tests/test_methods.py",
    "tests/test_streaming_visualization.py",
]
acceptance = [
    "Documentation and metadata state whether the offline raw SLAM cloud is the upstream confidence-masked fused PLY or a repo-materialized union of accepted streaming pointmaps.",
    "Final dense-cloud artifacts expose confidence-mask coverage diagnostics such as input keyframes, raster pixels, retained points, threshold, and retained fraction.",
    "If benchmark evaluation needs streaming-pointmap coverage, the repo materializes a separate pointmap-derived artifact instead of overloading `/world/slam/vista_slam_world/point_cloud/raw`.",
    "A regression covers the 18:42 ADVIO-20 case where the native confidence mask retained about 3.0M of 22.8M possible pointmap pixels.",
]
sources = [
    ".artifacts/vista-full-tuning/vista/native/confs.npz",
    ".artifacts/vista-full-tuning/vista/native/pointcloud.ply",
    ".artifacts/vista-full-tuning/vista/slam/point_cloud.ply",
    ".logs/runs/vista-full-tuning/2026-05-31_18:42:24_vista-full-tuning.log",
    "external/vista-slam/vista_slam/slam.py",
    "src/prml_vslam/methods/vista/session.py",
    "src/prml_vslam/methods/vista/artifacts.py",
]

[[todos]]
id = "TODO-0093"
title = "Reconcile `vista-full.toml` with the canonical full-pipeline modality"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0087",
]
loc_min = 12
loc_expected = 35
loc_max = 100
files = [
    ".configs/pipelines/vista-full.toml",
    ".configs/pipelines/vista-full-record3d-wifi.toml",
    "tests/test_pipeline_config.py",
]
acceptance = [
    "`tests/test_pipeline_config.py::test_vista_full_target_toml_parses_through_run_config` passes without weakening its role as the canonical full-config regression.",
    "If `vista-full.toml` remains the stable TUM-RGBD full-pipeline target, restore its TUM source, realtime/frame-stride, ground-alignment, mesh, trajectory-eval, and decimation expectations.",
    "If ADVIO-20 is intentionally promoted to the canonical full config, rename or rewrite the test, experiment name, and any local tuning assumptions together so the modality contract is explicit.",
    "ADVIO-20 tuning remains available through a separately named config when it is not the canonical default.",
]
sources = [
    ".configs/pipelines/vista-full.toml",
    "tests/test_pipeline_config.py",
    ".omx/ultragoal/brief.md",
]

[[todos]]
id = "TODO-0099"
title = "Align `align.cloud` planning availability with reference-cloud resolution"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0093",
]
loc_min = 20
loc_expected = 60
loc_max = 160
files = [
    "src/prml_vslam/eval/stage_cloud_alignment/config.py",
    "src/prml_vslam/eval/stage_cloud_alignment/spec.py",
    "src/prml_vslam/sources/datasets/advio/advio_geometry.py",
    "tests/test_advio.py",
    "tests/test_pipeline.py",
]
acceptance = [
    "Plan-time availability reflects the same reference options used by `_resolve_reference_cloud()` or records that source-cloud availability is source-preparation dependent.",
    "ADVIO high-RMS Tango runs that produce only source-native reference clouds do not plan `align.cloud` as available unless reconstruction can supply the reference cloud.",
    "A regression covers `align.cloud.enabled = true`, ADVIO source, rejected aligned Tango cloud, and reconstruction disabled.",
    "The user-facing availability reason says whether an aligned source cloud, reconstruction, or a policy choice is missing.",
]
sources = [
    "src/prml_vslam/eval/stage_cloud_alignment/config.py",
    "src/prml_vslam/eval/stage_cloud_alignment/spec.py",
    "src/prml_vslam/sources/datasets/advio/advio_geometry.py",
]

[[todos]]
id = "TODO-0100"
title = "Make the app's cloud-alignment control semantics explicit"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0094",
]
loc_min = 18
loc_expected = 45
loc_max = 120
files = [
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
    "src/prml_vslam/app/pipeline_controls.py",
    "src/prml_vslam/pipeline/config.py",
    "tests/test_app.py",
    "tests/test_pipeline_config.py",
]
acceptance = [
    "The Streamlit request state either exposes `align_cloud` explicitly or documents that dense-cloud evaluation in the app intentionally enables alignment as a coupled prerequisite.",
    "App request previews and generated `RunConfig` values round-trip the chosen cloud-alignment/evaluation semantics.",
    "CLI/TOML dotted overrides remain able to set `stages.align_cloud.*` directly without conflicting with app defaults.",
    "Tests cover the app path for enabling evaluation with and without explicit cloud-alignment intent.",
]
sources = [
    "src/prml_vslam/pipeline/config.py",
    "src/prml_vslam/app/models.py",
    "src/prml_vslam/app/pages/pipeline_request_editor.py",
]

[[todos]]
id = "TODO-0101"
title = "Remove local review artifacts and user-specific config from the reviewable branch"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0095",
]
loc_min = 3
loc_expected = 15
loc_max = 60
files = [
    ".codex/config.toml",
    "external/mast3r-slam/",
    "generate-unstaged-diff.sh",
    "unstaged-changes.diff",
]
acceptance = [
    "The reviewable branch no longer contains untracked `generate-unstaged-diff.sh` or `unstaged-changes.diff` artifacts.",
    "`external/mast3r-slam` is either intentionally represented as the repo's declared dependency surface or kept out of the branch as local checkout state.",
    "Checked-in Codex/MemPalace config does not contain a user-specific absolute `/home/jd/...` path unless the file is explicitly documented as user-local and excluded from review scope.",
    "`git status --short` separates intended source/backlog changes from local runtime or export debris.",
]
sources = [
    ".codex/config.toml",
    "external/mast3r-slam/",
    "generate-unstaged-diff.sh",
    "unstaged-changes.diff",
]

[meta]
schema_version = 2
repo = "prml-vslam"
updated_on = "2026-06-01"
source = "Seeded and refreshed from validated review findings including ADVIO Tango review, Rerun live viewer regression analysis, 2026-05-31 offline RRD inspection, Tango reference-cloud artifact inspection, current dirty-worktree code/config evidence, the 2026-05-31 18:42 cloud-evaluation run, consolidated GPT-5.5 Pro slop review, focused pytest, focused mypy evidence, and 2026-06-01 dual-base code-review findings"
active_collection = "todos"
loc_unit = "lines"

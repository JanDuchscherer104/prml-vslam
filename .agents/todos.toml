[[todos]]
id = "TODO-0032"
title = "Extend the evo adapter with explicit alignment and drift evaluation"
status = "pending"
priority = "medium"
issue_ids = [
    "ISSUE-0030",
]
loc_min = 45
loc_expected = 115
loc_max = 240
files = [
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/eval/contracts.py",
    "src/prml_vslam/app/pages/metrics.py",
    "src/prml_vslam/eval/README.md",
]
acceptance = [
    "Persisted trajectory metrics include explicit evaluation semantics such as alignment mode and metric type.",
    "The repo can compute drift-oriented RPE alongside or instead of translation-only APE through the existing thin evo seam.",
    "The metrics page continues to keep evaluation explicit and can render the richer persisted result shape.",
]
sources = [
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/eval/contracts.py",
    "src/prml_vslam/app/pages/metrics.py",
    "README.md",
]

[[todos]]
id = "TODO-0033"
title = "Replace generic packet metadata and duplicate transport enums with typed provenance"
status = "pending"
priority = "medium"
issue_ids = [
    "ISSUE-0031",
]
loc_min = 40
loc_expected = 95
loc_max = 190
files = [
    "src/prml_vslam/interfaces/runtime.py",
    "src/prml_vslam/io/cv2_producer.py",
    "src/prml_vslam/datasets/advio/advio_replay_adapter.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "tests/test_app.py",
    "tests/test_pipeline.py",
]
acceptance = [
    "`FramePacket` no longer exposes a generic provenance `metadata` dict on the shared hot path.",
    "Replay, dataset, rotation, and transport provenance remain available through typed fields or dedicated runtime-event data.",
    "Pipeline and app/io stop duplicating Record3D transport identity across separate enums when one shared transport type would suffice.",
]
sources = [
    "src/prml_vslam/interfaces/runtime.py",
    "src/prml_vslam/io/cv2_producer.py",
    "src/prml_vslam/datasets/advio/advio_replay_adapter.py",
    "src/prml_vslam/pipeline/contracts/request.py",
]

[[todos]]
id = "TODO-0034"
title = "Quarantine the Record3D Wi-Fi preview stack behind an experimental boundary"
status = "pending"
priority = "medium"
issue_ids = [
    "ISSUE-0032",
]
loc_min = 25
loc_expected = 65
loc_max = 140
files = [
    "src/prml_vslam/io/wifi_session.py",
    "src/prml_vslam/io/wifi_receiver.py",
    "src/prml_vslam/io/wifi_packets.py",
    "src/prml_vslam/io/README.md",
    "README.md",
]
acceptance = [
    "The optional Wi-Fi preview code is documented and structured as experimental or preview-only rather than a first-class ingestion surface.",
    "Core pipeline and app contracts do not depend on Wi-Fi-specific transport behavior beyond an optional factory boundary.",
    "The README and package docs make the reduced support level and intended scope unambiguous.",
]
sources = [
    "src/prml_vslam/io/wifi_session.py",
    "src/prml_vslam/io/wifi_receiver.py",
    "src/prml_vslam/io/wifi_packets.py",
    "src/prml_vslam/io/README.md",
    "README.md",
]

[[todos]]
id = "TODO-0035"
title = "Re-triage partially addressed resolved records against the current code"
status = "pending"
priority = "low"
issue_ids = [
    "ISSUE-0033",
]
loc_min = 10
loc_expected = 25
loc_max = 60
files = [
    ".agents/issues.toml",
    ".agents/todos.toml",
    ".agents/resolved.toml",
]
acceptance = [
    "Resolved archive entries that are only partially addressed are either amended with narrower resolution notes or reopened as active issues.",
    "The active backlog and resolved archive no longer imply that today’s still-open findings are fully closed.",
    "`make agents-db` reflects the intended active work after the re-triage pass.",
]
sources = [
    ".agents/issues.toml",
    ".agents/todos.toml",
    ".agents/resolved.toml",
]

[[todos]]
id = "TODO-0036"
title = "Extend stage-result execution to offline and remaining planned stages"
status = "pending"
priority = "high"
issue_ids = [
    "ISSUE-0034",
]
loc_min = 80
loc_expected = 180
loc_max = 380
files = [
    "src/prml_vslam/pipeline/contracts/execution.py",
    "src/prml_vslam/pipeline/offline.py",
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/pipeline/run_service.py",
    "tests/test_pipeline.py",
]
acceptance = [
    "Streaming mode keeps the process-capable stage-result execution path already implemented.",
    "Offline execution stops depending on `ingest_started`/`slam_started` booleans and consumes explicit stage results to build manifests and summaries.",
    "Reference reconstruction, cloud evaluation, and efficiency stages either remain explicitly unsupported or gain real executors with typed stage results.",
    "Stage manifests and run summaries are built from explicit stage results across every executable mode.",
]
sources = [
    "src/prml_vslam/pipeline/REQUIREMENTS.md",
    "src/prml_vslam/pipeline/contracts/execution.py",
    "src/prml_vslam/pipeline/offline.py",
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/pipeline/run_service.py",
]

[[todos]]
id = "TODO-0037"
title = "Consolidate remaining reusable frame-materialization helpers out of method-wrapper files"
status = "pending"
priority = "medium"
issue_ids = [
    "ISSUE-0035",
]
loc_min = 25
loc_expected = 65
loc_max = 140
files = [
    "src/prml_vslam/pipeline/ingest.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "tests/test_pipeline.py",
    "tests/test_methods.py",
]
acceptance = [
    "Generic video-frame materialization is owned once and reused by offline ingest and method adapters where fallback extraction remains necessary.",
    "The Pipeline page helper cleanup is treated as already done unless new drift appears; request/source helpers are app-controller owned and evo/pointmap preview helpers are eval/plotting owned.",
    "Python LOC decreases or stays neutral while focused tests continue covering the moved behavior.",
]
sources = [
    "src/prml_vslam/utils/REQUIREMENTS.md",
    "src/prml_vslam/pipeline/ingest.py",
    "src/prml_vslam/methods/vista/adapter.py",
]

[[todos]]
id = "TODO-0038"
title = "Fix Rerun pose-direction logging and add a world-space round-trip geometry test"
status = "pending"
priority = "high"
issue_ids = [
    "ISSUE-0036",
]
loc_min = 20
loc_expected = 60
loc_max = 140
files = [
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/streaming.py",
    "tests/test_streaming_visualization.py",
    "src/prml_vslam/visualization/README.md",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
]
acceptance = [
    "Repo `T_world_camera` poses are logged with parent-from-child semantics or an explicitly equivalent inverted-transform path.",
    "The ViSTA viewer-basis adapter remains explicit and singular instead of compensating for a wrong transform relation downstream.",
    "A focused visualization test round-trips a known camera-local point through a known pose and fails if world placement is mirrored, inverted, or displaced.",
    "Existing streaming visualization tests continue to cover entity layout and timeline behavior.",
]
sources = [
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/streaming.py",
    "tests/test_streaming_visualization.py",
    "src/prml_vslam/visualization/README.md",
]

[[todos]]
id = "TODO-0039"
title = "Extend live visualization updates to carry intrinsics, metric depth, and camera-complete Rerun payloads"
status = "pending"
priority = "medium"
issue_ids = [
    "ISSUE-0037",
]
loc_min = 60
loc_expected = 150
loc_max = 320
files = [
    "src/prml_vslam/methods/updates.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/streaming_coordinator.py",
    "tests/test_methods.py",
    "tests/test_streaming_visualization.py",
]
acceptance = [
    "`SlamUpdate` can carry camera intrinsics plus a metric depth raster or another explicit typed depth payload for live keyframes.",
    "The ViSTA live adapter no longer drops `view.depth` and `view.intri` after requesting them from upstream.",
    "Repo-owned viewer logging can emit `Pinhole` and `DepthImage` under camera entities and keep diagnostic previews separate from raw RGB and metric-depth semantics.",
    "The resulting viewer tree supports frustum-aligned debugging of camera-local pointmaps against depth and intrinsics for live keyframes.",
]
sources = [
    "src/prml_vslam/methods/updates.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/pipeline/streaming_coordinator.py",
    "tests/test_methods.py",
    "external/vista-slam/run.py",
]

[meta]
schema_version = 2
repo = "prml-vslam"
updated_on = "2026-04-16"
source = "Seeded from validated review findings, then expanded with the current-state import of the older PR15 review, the 2026-04-06 merged re-review of the surviving PR15 findings, the 2026-04-15 architecture simplification review, the Pipeline page helper cleanup, the 2026-04-16 streaming execution state review, and the 2026-04-16 Rerun integration review."
active_collection = "todos"
loc_unit = "lines"

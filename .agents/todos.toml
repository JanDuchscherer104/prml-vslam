[[todos]]
id = "TODO-0032"
title = "Extend the evo adapter with explicit alignment and drift evaluation"
status = "in_progress"
priority = "medium"
issue_ids = [
    "ISSUE-0030",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 45
loc_expected = 115
loc_max = 240
files = [
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/eval/contracts.py",
    "src/prml_vslam/app/pages/metrics.py",
    "src/prml_vslam/eval/README.md",
]
acceptance = [
    "Persisted trajectory metrics include explicit evaluation semantics such as alignment mode, metric id, and sync tolerance.",
    "The repo can compute drift-oriented RPE alongside or instead of translation-only APE through the existing thin evo seam.",
    "The metrics page continues to keep evaluation explicit and can render the richer persisted result shape.",
]
sources = [
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/eval/contracts.py",
    "src/prml_vslam/app/pages/metrics.py",
    "README.md",
]

[[todos]]
id = "TODO-0036"
title = "Harden the real Ray end-to-end smoke path after the coordinator-only backend cutover"
status = "in_progress"
priority = "high"
issue_ids = [
    "ISSUE-0034",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 90
loc_expected = 200
loc_max = 420
files = [
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/pipeline/README.md",
    "tests/test_pipeline.py",
]
acceptance = [
    "`RayPipelineBackend` owns coordinator lifecycle directly, and deterministic tests cover that submission/readback path.",
    "The remaining real offline and streaming Ray smoke tests pass under a bounded timeout without hanging inside `ray.get(...)` or worker startup.",
    "The Ray smoke coverage is no longer opt-in-only for everyday regression detection, or a dedicated always-on CI lane covers it explicitly.",
]
sources = [
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/run_service.py",
    "tests/test_pipeline.py",
]

[[todos]]
id = "TODO-0054"
title = "Reproduce the Rerun point-cloud/world-frame regression against a ViSTA-style reference recording"
status = "in_progress"
priority = "high"
issue_ids = [
    "ISSUE-0053",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 70
loc_expected = 170
loc_max = 360
files = [
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/pipeline/sinks/rerun_policy.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/visualization/README.md",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
    ".agents/AGENTS_INTERNAL_DB.md",
    "tests/test_streaming_visualization.py",
    "tests/test_rerun_timeline_semantics.py",
    "tests/test_rerun_layout_semantics.py",
]
acceptance = [
    "The repo has one focused regression harness that compares world placement from the repo-owned sink against a minimal local ViSTA-style recording with the same pose and camera-local pointmap payloads.",
    "The active Rerun guidance surfaces (`visualization/README.md`, `visualization/REQUIREMENTS.md`, and any stable memory kept in `.agents/AGENTS_INTERNAL_DB.md`) agree on one root-world convention policy and one entity-layout policy.",
    "A known camera-local test point and a dense pointmap land in the same world-space pose in the repo-owned recording as they do in the ViSTA-style reference recording.",
    "The final fix keeps camera-local pointmaps camera-local until composed through the intended transform hierarchy and does not rely on viewer-only basis flips that contradict the chosen repo policy.",
]
sources = [
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/pipeline/sinks/rerun_policy.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
    ".agents/AGENTS_INTERNAL_DB.md",
    "external/vista-slam/run.py",
    "external/vista-slam/run_live.py",
    "tests/test_streaming_visualization.py",
    "tests/test_rerun_layout_semantics.py",
]

[[todos]]
id = "TODO-0063"
title = "Complete WP-09C live events and handle cutover after the mid-flight DTO move"
status = "in_progress"
priority = "high"
issue_ids = [
    "ISSUE-0063",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 180
loc_expected = 420
loc_max = 850
files = [
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/ray_runtime/common.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/backend.py",
    "src/prml_vslam/pipeline/backend_ray.py",
    "tests/test_pipeline.py",
]
acceptance = [
    "Production imports remain green after the durable telemetry event removal.",
    "Coordinator packet handling accepts `FramePacket` plus `TransientPayloadRef` metadata and projects live source/SLAM state only through `StageRuntimeUpdate`.",
    "Durable `RunEvent` remains lifecycle/provenance-only and JSONL no longer filters on `EventTier`.",
    "`ArrayHandle`, `PreviewHandle`, `translate_slam_update()`, and `LegacySlamUpdate` are removed from production and tests.",
    "Current-facing docs and tests no longer describe backend-event translation as the live-observer path.",
    "Rerun and app payload reads use `read_payload()` with `TransientPayloadRef` only.",
]
sources = [
    "docs/architecture/pipeline-refactor-work-packages/WP-09C-live-events-handles-cutover.md",
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
]

[[todos]]
id = "TODO-0064"
title = "Complete WP-09A RunConfig launch cutover and retire RunRequest compatibility"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0063",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 260
loc_expected = 700
loc_max = 1400
files = [
    "src/prml_vslam/pipeline/config.py",
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/contracts/plan.py",
    "src/prml_vslam/pipeline/stage_registry.py",
    "src/prml_vslam/pipeline/source_resolver.py",
    "src/prml_vslam/pipeline/backend.py",
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/main.py",
    "src/prml_vslam/app/pipeline_controls.py",
    "tests/test_pipeline_config.py",
    "tests/test_main.py",
    "tests/test_app.py",
]
acceptance = [
    "App, CLI, `RunService`, and `PipelineBackend` submit `RunConfig` or compiled `RunPlan`, never `RunRequest`.",
    "`StageRegistry`, `OfflineSourceResolver`, request source variants, request SLAM config, placement projection helpers, `from_run_request()`, `to_run_request()`, `build_run_request()`, and `load_run_request_toml()` are removed from production.",
    "All committed `.configs/pipelines/*.toml` parse as target `RunConfig` and `RunPlan` contains target public stage keys.",
]
sources = [
    "docs/architecture/pipeline-refactor-work-packages/WP-09A-target-config-launch-cutover.md",
    "docs/architecture/pipeline-dto-migration-ledger.md",
]

[[todos]]
id = "TODO-0065"
title = "Complete WP-09D snapshot and app preview cleanup"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0063",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 120
loc_expected = 300
loc_max = 650
files = [
    "src/prml_vslam/pipeline/contracts/runtime.py",
    "src/prml_vslam/pipeline/snapshot_projector.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/preview_runtime.py",
    "src/prml_vslam/app/services.py",
    "src/prml_vslam/app/pages/datasets.py",
    "src/prml_vslam/app/pages/record3d.py",
    "tests/test_app_services.py",
    "tests/test_app.py",
    "tests/test_pipeline.py",
]
acceptance = [
    "No app, CLI, or test caller reads `RunSnapshot.stage_status` or `RunSnapshot.stage_progress`.",
    "Pipeline UI derives status from `stage_outcomes`, `stage_runtime_status`, plan rows, and current-stage context.",
    "App preview-only stale names such as `latest_packet`, `received_frames`, and `trajectory_positions_xyz` are renamed to preview-specific fields so WP-10 stale greps are unambiguous.",
]
sources = [
    "docs/architecture/pipeline-refactor-work-packages/WP-09D-app-cli-target-snapshot-cutover.md",
    "src/prml_vslam/pipeline/contracts/runtime.py",
    "src/prml_vslam/app/preview_runtime.py",
]

[[todos]]
id = "TODO-0066"
title = "Clean up ArtifactRef documentation after ownership migration"
status = "open"
priority = "medium"
issue_ids = [
    "ISSUE-0063",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 10
loc_expected = 35
loc_max = 90
files = [
    "src/prml_vslam/pipeline/contracts/provenance.py",
    "src/prml_vslam/interfaces/slam.py",
    "src/prml_vslam/interfaces/__init__.py",
    "src/prml_vslam/interfaces/visualization.py",
    "src/prml_vslam/methods/README.md",
    "tests/test_package_exports.py",
]
acceptance = [
    "`ArtifactRef` is owned and exported by `pipeline.contracts.provenance`.",
    "`interfaces.slam` keeps only `SlamArtifacts` and does not export generic artifact refs or live method DTOs.",
    "Source and tests import `ArtifactRef` from the provenance owner.",
    "Current-facing docs no longer point implementers at `interfaces.slam.ArtifactRef` or describe `ArtifactRef` as SLAM-owned.",
]
sources = [
    "docs/architecture/pipeline-dto-migration-ledger.md",
    "docs/architecture/pipeline-stage-refactor-target.md",
]

[[todos]]
id = "TODO-0067"
title = "Perform final WP-10 stale-symbol sweep and TargetStageKey to StageKey rename"
status = "open"
priority = "high"
issue_ids = [
    "ISSUE-0063",
]
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
loc_min = 150
loc_expected = 360
loc_max = 800
files = [
    "src/prml_vslam/pipeline/contracts/stages.py",
    "src/prml_vslam/pipeline/config.py",
    "src/prml_vslam/pipeline/README.md",
    "src/prml_vslam/*/REQUIREMENTS.md",
    "tests/",
]
acceptance = [
    "`TargetStageKey` is renamed to canonical `StageKey` only after blockers are removed.",
    "Old current-stage enum values and aliases are removed from production outputs.",
    "Stale-symbol greps are clean for retired request/config/event/handle/snapshot names outside historical docs and completion notes.",
    "`make ci` passes before the final cleanup commit.",
]
sources = [
    "docs/architecture/pipeline-refactor-work-packages/WP-10-migration-removal.md",
    "docs/architecture/pipeline-dto-migration-ledger.md",
]

[meta]
schema_version = 2
repo = "prml-vslam"
updated_on = "2026-04-23"
source = "Seeded from validated review findings, then expanded with the current-state import of the older PR15 review, the 2026-04-06 merged re-review of the surviving PR15 findings, the 2026-04-15 architecture simplification review, the Pipeline page helper cleanup, the 2026-04-16 streaming execution state review, the 2026-04-16 Rerun integration review, the 2026-04-16 current-worktree Streamlit/Ray test review, the 2026-04-16 validated Streamlit ViSTA request round-trip bug, the 2026-04-17 anti-pattern review covering typed seam erosion, ViSTA integration hygiene, duplicate transform DTOs, and leaf-module ownership drift, the 2026-04-17 repo inspection pass mapping those findings to concrete `prml_vslam` module seams, the 2026-04-17 follow-up pipeline refactor review persisting coordinator restart, stage-failure, terminal-state, placement-policy, and Rerun timeline defects, the 2026-04-18 Rerun point-cloud/world-frame regression audit against the current sink, persisted repo memory, the repo-local ViSTA reference, the 2026-04-23 WP-10 blocker inventory after the partial migration cutover, and the 2026-04-23 stabilization pass narrowing active blockers after import smoke turned green."
active_collection = "todos"
loc_unit = "lines"

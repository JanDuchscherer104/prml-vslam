[[issues]]
id = "ISSUE-0030"
title = "Trajectory evaluation still lacks explicit alignment and drift semantics"
status = "in_progress"
priority = "high"
category = "evaluation"
summary = "Persisted trajectory evaluation now records explicit metric semantics, but the evaluator still computes only translation APE and does not yet expose drift-oriented RPE or alternative alignment modes beyond timestamp association."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/sources/contracts.py",
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/eval/contracts.py",
    "src/prml_vslam/pipeline/finalization.py",
    "README.md",
    "docs/Questions.md",
]
notes = [
    "The public config already separates trajectory evaluation enablement from `baseline_source`, so the old enablement-vs-baseline conflation is resolved.",
    "The evaluator now persists explicit semantics such as metric id, pose relation, alignment mode, and sync tolerance, which closes the earlier ambiguity in the stored payload.",
    "The remaining gap is executable drift-oriented RPE and richer alignment choices through the same thin evo seam, which still matters because trajectory quality is the top evaluation priority.",
]
sources = [
    "src/prml_vslam/sources/contracts.py",
    "src/prml_vslam/eval/services.py",
    "src/prml_vslam/eval/contracts.py",
    "src/prml_vslam/pipeline/finalization.py",
    "README.md",
    "docs/Questions.md",
]

[[issues]]
id = "ISSUE-0034"
title = "The real Ray end-to-end smoke path remains environment-sensitive after the coordinator-only backend cutover"
status = "in_progress"
priority = "high"
category = "orchestration"
summary = "The backend now owns coordinator creation directly and has deterministic unit coverage for coordinator submission and lookup, but the real Ray end-to-end smoke path is still opt-in because cluster startup and worker execution remain environment-sensitive."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/pipeline/README.md",
    "tests/test_pipeline.py",
]
notes = [
    "`RayPipelineBackend.submit_run()` now creates the named coordinator directly, and deterministic tests cover coordinator-backed submission plus snapshot and event reads.",
    "The remaining problem is not the control-plane shape but the real local-cluster execution path, which is still expensive and can hang in this environment when the opt-in smoke tests are forced.",
    "The default pytest suite still keeps the real Ray smokes opt-in while the environment-sensitive startup path is not trustworthy enough for always-on coverage.",
]
sources = [
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/pipeline/README.md",
    "tests/test_pipeline.py",
]

[[issues]]
id = "ISSUE-0053"
title = "Repo-owned Rerun point-cloud/world-frame semantics regressed after the policy-extraction cleanup"
status = "in_progress"
priority = "high"
category = "visualization"
summary = "The current sink keeps ViSTA pointmaps camera-local in contracts, but the live viewer path now shows clouds clustered near the origin again and the repo's persisted Rerun guidance is internally inconsistent about root-world convention, entity layout, and what behavior is considered the 'working' reference."
wip_branch = "codex/streaming-execution-architecture"
wip_worktree = "/home/jandu/repos/prml-vslam"
files = [
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/pipeline/sinks/rerun_policy.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/visualization/README.md",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
    ".agents/AGENTS_INTERNAL_DB.md",
    "external/vista-slam/run.py",
    "external/vista-slam/run_live.py",
    "tests/test_streaming_visualization.py",
    "tests/test_rerun_timeline_semantics.py",
    "tests/test_rerun_layout_semantics.py",
]
notes = [
    "The current ViSTA adapter still emits camera-local pointmaps from `get_pointmap_vis(...)` and canonical repo `T_world_camera` poses, so the regression is not explained by payload generation alone.",
    "The current sink logs explicit point clouds under `world/live/model/points` and `world/keyframes/<id>/points` beneath shared posed parents, which is structurally closer to upstream ViSTA than the older repo-owned posed-pointmap-branch layout.",
    "The current persisted guidance is inconsistent: `visualization/REQUIREMENTS.md` says the ViSTA-aligned path should preserve upstream-native world semantics and keep camera-local pointmaps composed through posed pointmap branches, while `.agents/AGENTS_INTERNAL_DB.md` still records a 'stable lesson' that the live Rerun root world should be declared as `ViewCoordinates.RDF`.",
    "Upstream ViSTA logs `/world` as an identity `Transform3D`, logs the pose directly on `world/est/<topic>`, logs `Pinhole(..., camera_xyz=RDF)` on `world/est/<topic>/cam`, and logs camera-local `Points3D` on `world/est/<topic>/points`; it does not log root `ViewCoordinates`, explicit `DepthImage`, source-vs-model branches, or a separate tracking branch.",
    "The current tests validate helper math and mocked event ordering, but they do not exercise actual Rerun transform inheritance or compare the repo-owned recording against a minimal ViSTA-style recording with the same pose and pointmap payloads.",
]
sources = [
    "src/prml_vslam/pipeline/sinks/rerun.py",
    "src/prml_vslam/pipeline/sinks/rerun_policy.py",
    "src/prml_vslam/visualization/rerun.py",
    "src/prml_vslam/methods/vista/adapter.py",
    "src/prml_vslam/visualization/REQUIREMENTS.md",
    ".agents/AGENTS_INTERNAL_DB.md",
    "external/vista-slam/run.py",
    "external/vista-slam/run_live.py",
    "codex-session-019d9b63-user-messages.md",
    "codex-session-019d9b63-agent-messages.md",
]

[[issues]]
id = "ISSUE-0055"
title = "Stage config and runtime ownership is still split across migration-era surfaces"
status = "open"
priority = "high"
category = "orchestration"
summary = "The target architecture defines stage-local configs, runtimes, proxies, a runtime manager, and a result store, but the current executable path still has stage identity, planning, source policy, runtime invocation, failure provenance, and observer routing split across pipeline configs, coordinator code, runtime scaffolding, and legacy compatibility seams."
files = [
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "docs/architecture/pipeline-stage-refactor-target.md",
    "docs/architecture/pipeline-dto-migration-ledger.md",
    "src/prml_vslam/pipeline/config.py",
    "src/prml_vslam/pipeline/runtime_manager.py",
    "src/prml_vslam/pipeline/runner.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/stages/base/config.py",
    "src/prml_vslam/pipeline/stages/base/proxy.py",
]
notes = [
    "The target docs make `RunConfig`, `StageBundle`, `StageConfig`, `RuntimeManager`, `StageRuntimeProxy`, `StageRunner`, and `StageResultStore` the ownership model for stages.",
    "The worktree has already introduced several target modules, but production execution still preserves compatibility hooks and coordinator-side orchestration that keep the stage abstraction hard to discover from one module.",
    "This issue tracks the architectural debt separately from narrow implementation bugs so future docstrings and refactor work can point readers to the target ownership model without pretending the migration is complete.",
]
sources = [
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "docs/architecture/pipeline-stage-refactor-target.md",
    "docs/architecture/pipeline-refactor-work-packages/README.md",
    "src/prml_vslam/pipeline/REQUIREMENTS.md",
]

[[issues]]
id = "ISSUE-0058"
title = "Source construction still has parallel compatibility paths instead of one source backend config seam"
status = "open"
priority = "medium"
category = "configuration"
summary = "The target architecture wants `SourceStageConfig` plus `SourceBackendConfig` variants as the source selection seam, but current compatibility still bridges `SourceSpec`, manual resolver logic, target source configs, and demo/runtime-source helpers."
files = [
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/source_resolver.py",
    "src/prml_vslam/pipeline/demo.py",
    "src/prml_vslam/pipeline/config.py",
    "src/prml_vslam/pipeline/stages/source/config.py",
    "src/prml_vslam/pipeline/stages/source/runtime.py",
    "src/prml_vslam/datasets/advio/advio_service.py",
    "src/prml_vslam/io/record3d_source.py",
]
notes = [
    "The present-state audit identifies source muxing as inconsistent with method backend muxing.",
    "The target source config classes exist, but old `SourceSpec` compatibility and resolver helpers remain active until WP-04/WP-09/WP-10 cutovers finish.",
    "This is medium priority because the current path is functional, but adding a new source still requires understanding several compatibility surfaces.",
]
sources = [
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "docs/architecture/pipeline-stage-refactor-target.md",
    "docs/architecture/pipeline-dto-migration-ledger.md",
    "src/prml_vslam/pipeline/stages/source/config.py",
]

[[issues]]
id = "ISSUE-0059"
title = "Resource placement remains split between typed target config and legacy Ray option policy"
status = "open"
priority = "medium"
category = "runtime"
summary = "Target `ResourceSpec`, `PlacementConstraint`, and `StageExecutionConfig` now document substrate-neutral stage policy, but legacy `StagePlacement`, `PlacementPolicy`, and Ray option translation remain in compatibility paths."
files = [
    "src/prml_vslam/pipeline/contracts/request.py",
    "src/prml_vslam/pipeline/placement.py",
    "src/prml_vslam/pipeline/config.py",
    "src/prml_vslam/pipeline/stages/base/config.py",
    "src/prml_vslam/pipeline/stages/base/proxy.py",
    "src/prml_vslam/pipeline/stages/base/ray.py",
]
notes = [
    "The target config model makes stage-local execution policy declarative and substrate-neutral.",
    "The current code still has legacy Ray-oriented placement aliases and projection helpers until the runtime proxy owns Ray deployment plumbing.",
    "Public docs and docstrings should make clear that target stage configs do not construct actors and that Ray refs stay behind runtime/proxy plumbing.",
]
sources = [
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "docs/architecture/pipeline-stage-refactor-target.md",
    "docs/architecture/pipeline-refactor-work-packages/WP-02-config-planning.md",
    "src/prml_vslam/pipeline/stages/base/config.py",
]

[[issues]]
id = "ISSUE-0060"
title = "Generic serialization and artifact references have diffuse ownership"
status = "open"
priority = "medium"
category = "architecture"
summary = "Generic helpers such as `stable_hash` and artifact-reference DTOs are still owned by pipeline finalization or SLAM interfaces even though multiple packages depend on them as generic serialization/provenance surfaces."
files = [
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/pipeline/ingest.py",
    "src/prml_vslam/interfaces/slam.py",
    "src/prml_vslam/methods/vista/artifacts.py",
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/contracts/provenance.py",
]
notes = [
    "The present-state audit calls out `stable_hash` and `write_json` living in pipeline finalization while method code imports them.",
    "The DTO migration ledger marks `ArtifactRef` as a generic artifact/provenance concept that should move out of `interfaces.slam` after imports migrate.",
    "This issue is not about changing behavior immediately; it records a public-interface ownership mismatch that docstrings should not hide.",
]
sources = [
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "docs/architecture/pipeline-dto-migration-ledger.md",
    "src/prml_vslam/pipeline/finalization.py",
    "src/prml_vslam/interfaces/slam.py",
]

[[issues]]
id = "ISSUE-0061"
title = "ViSTA normalized artifacts still lack typed intrinsics and preprocessing side metadata"
status = "open"
priority = "medium"
category = "artifacts"
summary = "Offline ViSTA runs preserve native arrays as extras, but estimated intrinsics, source-to-model raster preprocessing, and color-preserving point-cloud normalization are not yet standardized as typed repo artifacts with explicit raster and frame semantics."
files = [
    "src/prml_vslam/methods/vista/artifacts.py",
    "src/prml_vslam/methods/vista/preprocess.py",
    "src/prml_vslam/interfaces/camera.py",
    "src/prml_vslam/utils/geometry.py",
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "docs/architecture/pipeline-stage-refactor-target.md",
]
notes = [
    "The target architecture assigns method-native artifact interpretation and standardization to `methods.vista`, shared camera datamodels to `interfaces.camera`, and generic color-preserving PLY IO to `utils.geometry`.",
    "The present-state audit records that native `intrinsics.npy` lives in ViSTA model-raster space while input intrinsics can describe the source raster, so direct comparison is misleading without persisted preprocessing metadata.",
    "This debt matters for docstrings because camera/raster semantics are central to both human understanding and future evaluation work.",
]
sources = [
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "docs/architecture/pipeline-stage-refactor-target.md",
    "src/prml_vslam/methods/vista/artifacts.py",
    "src/prml_vslam/interfaces/camera.py",
]

[[issues]]
id = "ISSUE-0062"
title = "Visualization validation DTOs still live in an implementation helper module"
status = "open"
priority = "low"
category = "visualization"
summary = "Rerun validation summary DTOs are currently defined in `visualization.validation`, but the target ownership model wants viewer/export DTOs in visualization contract modules and implementation helpers to stay focused on validation execution."
files = [
    "src/prml_vslam/visualization/validation.py",
    "src/prml_vslam/visualization/contracts.py",
    "src/prml_vslam/visualization/README.md",
    "docs/architecture/pipeline-stage-present-state-audit.md",
]
notes = [
    "This is localized and low priority compared with runtime topology, but it is a real public-interface placement issue listed in the present-state audit.",
    "Keeping DTOs in implementation modules makes it harder for users and agents to discover the viewer artifact contract from the package's contract surface.",
]
sources = [
    "docs/architecture/pipeline-stage-present-state-audit.md",
    "src/prml_vslam/visualization/validation.py",
    "src/prml_vslam/visualization/contracts.py",
]

[[issues]]
id = "ISSUE-0063"
title = "Pipeline migration cutover is mid-flight and still blocks WP-10"
status = "in_progress"
priority = "high"
category = "orchestration"
summary = "The current worktree has repaired the immediate WP-09C import breakage after moving `ArtifactRef` to pipeline provenance, moving live SLAM DTOs toward `methods.contracts`, and removing telemetry variants from `RunEvent`, but it remains a mid-flight migration. WP-10 is not resolved until WP-09A, the remaining WP-09C migration-only symbols, WP-09D app preview cleanup, and the final stage-key rename are complete and stale-symbol greps are clean."
files = [
    "src/prml_vslam/pipeline/config.py",
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/contracts/runtime.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/pipeline/ray_runtime/stage_actors.py",
    "src/prml_vslam/pipeline/backend.py",
    "src/prml_vslam/pipeline/backend_ray.py",
    "src/prml_vslam/pipeline/run_service.py",
    "src/prml_vslam/app/pipeline_controls.py",
    "src/prml_vslam/app/pipeline_controller.py",
    "src/prml_vslam/app/preview_runtime.py",
    "src/prml_vslam/methods/contracts.py",
    "src/prml_vslam/interfaces/slam.py",
    "tests/",
]
notes = [
    "Import smoke for `prml_vslam.app.pipeline_controller`, `prml_vslam.pipeline.ray_runtime.coordinator`, `prml_vslam.pipeline.backend_ray`, `prml_vslam.pipeline.run_service`, and `prml_vslam.main` is green after the WP-09C repair.",
    "RunRequest launch compatibility remains widespread in app, CLI, backend, coordinator, stage input contracts, source resolver, and tests; `RunConfig` still projects through `to_run_request()` in production.",
    "Target stage-key vocabulary is not yet canonical: current `StageKey` values such as `ingest`, `trajectory.evaluate`, `reference.reconstruct`, `cloud.evaluate`, and `efficiency.evaluate` still drive production outputs and tests.",
    "Durable/live split is now lifecycle/provenance-only for `RunEvent`; old handle DTOs, `translate_slam_update()`, and `LegacySlamUpdate` have been removed from production and tests.",
    "Snapshot contract cleanup is mostly done for `RunSnapshot.stage_status` and `stage_progress`; app preview-only naming has been moved to preview-specific fields, while `RunSummary.stage_status` remains persisted provenance and is not a deletion target.",
]
sources = [
    "docs/architecture/pipeline-refactor-work-packages/WP-09A-target-config-launch-cutover.md",
    "docs/architecture/pipeline-refactor-work-packages/WP-09C-live-events-handles-cutover.md",
    "docs/architecture/pipeline-refactor-work-packages/WP-09D-app-cli-target-snapshot-cutover.md",
    "docs/architecture/pipeline-refactor-work-packages/WP-10-migration-removal.md",
    "docs/architecture/pipeline-dto-migration-ledger.md",
    "src/prml_vslam/pipeline/contracts/events.py",
    "src/prml_vslam/pipeline/ray_runtime/coordinator.py",
    "src/prml_vslam/app/pipeline_controller.py",
]

[meta]
schema_version = 2
repo = "prml-vslam"
updated_on = "2026-04-23"
source = "Validated review findings plus follow-up repository policy notes, the current-state import of the older PR15 review, the 2026-04-06 merged re-review of the surviving PR15 findings, the 2026-04-15 architecture simplification review, the Pipeline page helper cleanup, the 2026-04-16 streaming execution state review, the 2026-04-16 Rerun integration review, the 2026-04-16 current-worktree Streamlit/Ray test review, the 2026-04-16 validated Streamlit ViSTA request round-trip bug, the 2026-04-17 anti-pattern review covering typed seam erosion, ViSTA integration hygiene, duplicate transform DTOs, and leaf-module ownership drift, the 2026-04-17 repo inspection pass mapping those findings to concrete `prml_vslam` module seams, the 2026-04-17 follow-up pipeline refactor review persisting coordinator restart, stage-failure, terminal-state, placement-policy, and Rerun timeline defects, the 2026-04-18 Rerun point-cloud/world-frame regression audit against the current sink, the persisted session transcript, the repo-local ViSTA reference, the 2026-04-22 architecture/docstring pass over the pipeline stage refactor target, work-package docs, DTO migration ledger, graphify report, code-index results, current code migration contacts, the 2026-04-23 WP-10 blocker inventory after the partial migration cutover, and the 2026-04-23 stabilization pass confirming import smoke is green while WP-09A/WP-09C/WP-09D blockers remain."
active_collection = "issues"

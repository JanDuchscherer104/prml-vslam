# Batch-build canonical full-frame normalized dataset entries.
# Empty sequence_ids means every locally offline-ready scene for that dataset.
# max_workers defaults to all available CPU workers when omitted.
missing_policy = "skip"
overwrite = false

[[datasets]]
dataset_id = "advio"
sequence_ids = []

[[datasets]]
dataset_id = "tum_rgbd"
sequence_ids = []

[[datasets]]
dataset_id = "record3d"
sequence_ids = []

[datasets.reference_cloud]
depth_stride_px = 8
max_points = 100000
min_confidence = 1

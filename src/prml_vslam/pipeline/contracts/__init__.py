"""Pipeline-owned contract namespace.

This package contains the typed context, plan, event, runtime, artifact, and
provenance surfaces that the rest of :mod:`prml_vslam.pipeline` shares.
`RunConfig` is the persisted declarative root and lives in
:mod:`prml_vslam.pipeline.config`.
"""

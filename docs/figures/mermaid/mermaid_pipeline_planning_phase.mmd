flowchart TD
    config["RunConfig"]
    compile["RunConfig.compile_plan()"]
    stages["Select enabled StageConfig sections"]
    availability["Check stage availability()"]
    outputs["Assign declared_outputs()"]
    plan["RunPlan"]
    preview["Preview in app or CLI"]
    runtime["RunService preflight"]

    config -->|"persisted TOML"| compile
    compile --> stages
    stages --> availability
    availability --> outputs
    outputs --> plan
    plan -->|"preview"| preview
    plan -->|"start_run()"| runtime

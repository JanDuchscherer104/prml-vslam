= Experiments

The experimental plan is split into public-dataset benchmarking and custom-dataset benchmarking.
For each method, the benchmark should capture run configuration, produced artifacts, runtime cost,
and all trajectory or reconstruction metrics required by the final recommendation.

The initial scaffold in this repository intentionally prioritizes reproducibility over breadth. The
first milestone is a functional installable package, a stable evaluation layout, and documentation
that lets contributors add method-specific experiments without redesigning the repository.
